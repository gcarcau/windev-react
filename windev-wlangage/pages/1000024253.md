---
title: "<Table>.FixeColonne (Fonction)"
english: "<Table>.FixColumn"
type: "Fonction"
id: "1000024253"
module: "WDLang1"
category: "fonctions_table"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# <Table>.FixeColonne (Fonction)

> **Disponible :** **WINDEV** (Windows, Etats et Requêtes, Code Utilisateur (MCU))

Fixe une colonne à gauche ou libère une colonne fixée à gauche. Cette fonction permet également de savoir si la colonne est fixée à gauche ou non.
**Remarques** :

- Les colonnes fixées à gauche ne scrollent pas avec l'ascenseur horizontal.
- L'appel de cette fonction est équivalente à :
  - l'usage du cadenas par l'utilisateur.
  - l'usage de l'option "Fixée à gauche" du menu contextuel du titre de la colonne.
   Pour plus de détails, consultez [FAA : Fixer les colonnes d'un champ Table](9500109.md)

## Exemple

```wlangage
// Fixe/Libère une colonne avec un bouton
// Clic sur le bouton
SI COL_COLONNE.FixeColonne() ALORS
	// La colonne était fixée
	COL_COLONNE.FixeColonne(Faux) // On la libère
SINON
	// La colonne était libre
	COL_COLONNE.FixeColonne(Vrai) // On la fixe
FIN
```

## Syntaxe

```wlangage
<Résultat> = <Colonne>.FixeColonne([<Fixer / Libérer>])
```

`<Résultat>` *Booléen*
> Etat précédent de la colonne :
>
> - Vrai, colonne fixée,
> - Faux, colonne libre.
`<Colonne>` *Nom de champ*
> Nom de la colonne à manipuler.
`<Fixer / Libérer>` *Booléen optionnel*
> - Vrai : la colonne doit être fixée
> - Faux : la colonne doit être libérée.
>
> Si ce paramètre n'est pas précisé, la fonction **<Table>.FixeColonne** renvoie l'état actuel de la colonne.

## Remarques

- Si la fonction **<Table>.FixeColonne** est utilisée pour fixer une colonne déjà fixée, cette fonction n'a aucun effet.
- Cette fonction ne peut pas être utilisée avec des colonnes indicées.

**Composante : wd300obj.dll**

## Voir Aussi

- [Fonctions de gestion des champs Table (syntaxe préfixée)](1000024319.md)
- [TableFixeColonne (Fonction)](3074014.md)
