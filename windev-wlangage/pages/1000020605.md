---
title: "<Menu>.Existe (Fonction)"
english: "<Menu>.Exist"
type: "Fonction"
id: "1000020605"
module: "WDLang1"
category: "fonctions_menu"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# <Menu>.Existe (Fonction)

> **Disponible :** **WINDEV** (Windows, Linux, Etats et Requêtes, Code Utilisateur (MCU))

Indique si une option de menu existe dans un menu.

## Exemple

```wlangage
// Si l'option n'existe pas encore
SI MonMenu.Existe("OuvreClient" + ID) = Faux ALORS
	// Ajout de l'option
	MonMenu.AjouteOption("OuvreClient" + ID, "Ouvrir la fiche " + ID, OuvreFiche, ID)
FIN
```

## Syntaxe

```wlangage
<Résultat> = <Menu>.Existe(<Option de menu>)
```

`<Résultat>` *Booléen*
> - Vrai si l'option de menu existe,
> - Faux dans le cas contraire.
>
>
> **Attention** : La recherche n'est pas récursive : si le menu contient un sous-menu qui contient l'option, <Résultat> vaut Faux.
`<Menu>` *Nom de menu*
> Nom du menu ou du menu contextuel dans lequel la recherche doit être effectuée.
`<Option de menu>` *Chaîne de caractères*
> Nom de l'option de menu à rechercher.

**Composante : wd300obj.dll**

## Voir Aussi

- [Fonctions de gestion des menus (syntaxe préfixée)](1000020631.md)
- [MenuExiste (Fonction)](1000019365.md)
