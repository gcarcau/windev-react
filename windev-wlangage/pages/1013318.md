---
title: "Manipuler un champ Liste par programmation (syntaxe préfixée)"
type: "syntaxe préfixée"
id: "1013318"
module: "WDControls"
category: "champ_liste"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# Manipuler un champ Liste par programmation (syntaxe préfixée)

> **Disponible :** **WINDEV** (Windows, Linux, Java) · **WEBDEV** (Windows, Linux, PHP) · **WINDEV Mobile** (Android, iPhone/iPad)

## Présentation

WINDEV, WEBDEV et WINDEV Mobile permettent de manipuler un champ Liste directement par programmation. Il suffit d'utiliser directement la variable du champ Liste dans le code. Cette variable est de type numérique.
La variable du champ Liste :

- correspond au nom du champ Liste.
- est initialisée avec l'indice de l'élément sélectionné dans le champ Liste.

Pour manipuler un élément du champ Liste, il suffit d'utiliser la notation suivante :

```wlangage
<Champ Liste> [<Indice de l'élément>]
```

**Remarque** : De nombreuses fonctions du WLangage sont spécifiques à la gestion des champs de type Liste. Pour plus de détails, consultez [les fonctions de gestion des champs Liste](1000017419.md).

## Initialiser un champ Liste

### Initialiser un élément
Pour initialiser un élément d'un champ Liste, utilisez la fonction [<Liste>.Ajoute](1000017319.md).
**Remarque** : Pour modifier un élément d'un champ Liste, il est possible d'utiliser la syntaxe :

```wlangage
<Champ Liste>[Indice] = <valeur>
```

Par exemple :

```wlangage
// Ajout d'éléments
LISTE_LISTE1.Ajoute("DUPOND")
LISTE_LISTE1.Ajoute(Nom_Cli)
// Modification d'éléments
LISTE_LISTE1[3] = "DUPONT"
```

### Remplir un champ Liste
Pour initialiser un champ Liste, il suffit d'utiliser une boucle d'itération pour initialiser tous les éléments du champ Liste.

```wlangage
Indice est un entierPOUR Indice = <Position Début> A <Position Fin> <Champ Liste>.Ajoute(<Valeur>)FIN
```

Exemple :

```wlangage
i est un entier
POUR i = 1 À 52
 LISTE_LISTE1.Ajoute(NOM[i])
FIN
```

## Sélectionner un élément dans un champ Liste

### Sélectionner une ligne
Pour sélectionner l'élément de la ligne <Indice>, utilisez :

- soit l'affectation directe :
  ```wlangage
  <Champ Liste> = Indice
  ```
- soit la fonction [<Liste>.SelectPlus](1000017414.md) :
  ```wlangage
  <Champ Liste>.SelectPlus(<Indice>)
  ```

### Se positionner sur le dernier élément
Pour se positionner sur le dernier élément du champ Liste, utilisez :

- soit la propriété [Occurrence](3025009.md) :
  ```wlangage
  <Champ Liste>.Occurrence
  ```
- soit la fonction [<Liste>.SelectPlus](1000017414.md) associée à la propriété [Occurrence](3025009.md) :
  ```wlangage
  <Champ Liste>.SelectPlus(<Champ Liste>.Occurrence)
  ```
- soit la fonction [<Liste>.Occurrence](1000017334.md) :
  ```wlangage
  <Champ Liste>.Occurrence()
  ```

## Récupérer l'élément sélectionné dans un champ Liste

### Récupérer l'indice de l'élément sélectionné
Pour récupérer l'indice de l'élément sélectionné, utilisez :

- soit la lecture directe de l'élément :
  ```wlangage
  Indice = <Champ Liste>
  ```
- soit la fonction [<Liste>.Select](1000017336.md) :
  ```wlangage
  Indice = <Champ Liste>.Select()
  ```

**Remarque** : Pour les champs Liste multisélection, la fonction [<Liste>.Select](1000017336.md) permet de récupérer les éléments sélectionnés.

```wlangage
// Récupérer les éléments d'un champ Liste multisélection
Rang est un entier = 0  // numéro de la sélection
Indice est un entier = 0  // indice de l'élément sélectionné
BOUCLE
 Rang = Rang + 1
 Indice = LISTE_LISTE1.Select(Rang)
 SI Indice = -1 ALORS SORTIR
FIN
```

### Récupérer la valeur de l'élément sélectionné
Pour récupérer la valeur de l'élément sélectionné, utilisez une des syntaxes suivantes :

- **Syntaxe 1** :
  ```wlangage
  Indice est un entier = <Champ Liste><Variable> = <Champ Liste>[Indice]
  ```
- **Syntaxe 2** :
  ```wlangage
  <Variable> = <Champ Liste>[<Champ Liste>]
  ```
- **Syntaxe 3** : Propriété [ValeurAffichée](2510129.md) (en code serveur uniquement)
  ```wlangage
  <Variable> = <Champ Liste>[Indice].ValeurAffichée
  ```

**Attention** : La valeur mémorisée en programmation peut être différente de la valeur affichée. Pour connaître la valeur mémorisée correspondant à une des lignes du champ Liste, utilisez la propriété [ValeurMémorisée](2510127.md).

## Parcourir les éléments d'un champ Liste

L'instruction **POUR TOUT** permet d'effectuer différents types de parcours d'un champ Liste :

- Parcours des éléments du champ. La syntaxe utilisée est la suivante :
  ```wlangage
  POUR TOUTE LIGNE [<Indice> [, <Compteur>]] DE <Champ Liste>  ...FIN
  ```
   où :
  - <Indice> : Variable optionnelle de type Entier. A chaque itération, contient l'indice de la ligne en cours.
  - <Compteur> : Variable optionnelle de type Entier. Cette variable compte le nombre d'itérations.
- Parcours des éléments des éléments sélectionnés. La syntaxe utilisée est la suivante :
  ```wlangage
  POUR TOUTE LIGNE SELECTIONNEE [<Indice>, [<Compteur>]] DE <Champ Liste>    ...FIN
  ```
   où :
  - <Indice> : Variable de type Entier. A chaque itération, contient l'indice de la ligne sélectionnée en cours.
  - <Compteur> : Variable optionnelle de type Entier. Cette variable compte le nombre d'itérations (nombre de lignes sélectionnées par exemple).

Pour plus de détails, consultez [Instruction POUR TOUT / POUR TOUS (parcours de champs)](1510017.md).

## Propriétés spécifiques à la gestion des champs Liste

Les propriétés suivantes sont spécifiques à la gestion des champs Liste :

| **Tout type de champs Liste** |  |
| --- | --- |
| [CadrageHorizontal](2510023.md) | Permet de connaître et de modifier le cadrage horizontal des éléments du champ Liste. |
| [HauteurLigne](2510041.md) | Permet de connaître et de modifier la hauteur des lignes d'un champ Liste. |
| [Mémoire](2510076.md) | Permet de savoir si le champ spécifié est un champ Liste par programmation ou un champ Liste fichier. |
| [Occurrence](3025009.md) | Permet de connaître le nombre de lignes présentes dans un champ Liste. |
| [TypeRemplissage](1000017723.md) | Permet de connaître le type de remplissage (liaison fichier, par programmation, liaison variable) d'un champ de type Table, Liste, Liste image, Combo ou Zone répétée. |
| [ValeurAffichée](2510129.md) | Permet de connaître la valeur de la ligne en cours ou d'une ligne spécifique du champ Liste. |
| [ValeurMémorisée](2510127.md) | Permet de connaître la valeur actuellement mémorisée lors de la sélection d'une ligne du champ Liste. |
| [Vide](2510126.md) | Permet de savoir si un champ Liste est vide. |
| **Champ Liste par programmation uniquement** |  |
| [ContenuInitial](2510021.md) | Permet de connaître le contenu initial d'un champ Liste par programmation. |
| [Triée](2510115.md) | Permet de savoir si un champ Liste par programmation est triée et de trier un champ Liste par programmation. |
| **Champ Liste Fichier uniquement** |  |
| [FichierParcouru](2510040.md) | Permet de connaître et de modifier le fichier de données ou la requête utilisé pour l'affichage des enregistrements dans les champs Liste fichier. |
| [ParcoursAuto](2510093.md) | Permet de savoir si le parcours d'un champ Liste fichier est automatique ou programmé. |
| [RubriqueAffichée](2510123.md) | Permet de connaître ou de modifier la rubrique affichée dans un champ Liste. |
| [RubriqueMémorisée](2510110.md) | Permet de connaître ou de modifier la rubrique mémorisée d'un champ Liste. |
| [RubriqueParcourue](2510111.md) | Permet de connaître et de modifier la rubrique utilisée pour le parcours automatique des champs Liste. |

Pour connaître la liste complète des propriétés du WLangage pouvant être utilisées avec un champ de type Liste, consultez [Propriétés associées au champ Liste.](1013182.md)

## Voir Aussi

- [Le champ Liste](1013185.md)
- [Comment gérer une multisélection dans un champ Liste ?](20012.md)
- [Manipuler un champ Liste par programmation](1013176.md)
