---
title: "PicRotation (Fonction)"
english: "PicRotation"
type: "Fonction"
id: "1000024633"
module: "WDLang1"
category: "fonctions_pic"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# PicRotation (Fonction)

> **Disponible :** **WINDEV** (Windows, Etats et Requêtes, Code Utilisateur (MCU)) · **WEBDEV** (Windows)

Effectue une rotation d'une image au format WDPic. Tous les calques de l'image effectuent la rotation.
Remarque : Pour effectuer la rotation d'un seul calque, utilisez la fonction [dRotation](3029024.md).

## Exemple

```wlangage
// Ouverture d'une image source
MonImageWDPic est un WDPic
MonImageWDPic.NomFichier = fRepExe() + ["\"] + "Source.png"

// Rotation de l'image
PicRotation(MonImageWDPic, 45)

// Sauvegarde du résultat
PicSauve(MonImageWDPic, fRepExe() + ["\"] + "Resultat.png")
```

## Syntaxe

#### Effectuer une rotation simple

```wlangage
<Résultat> = PicRotation(<Image WDPic> , <Angle> [, <Options>])
```

`<Résultat>` *Booléen*
> - Vrai si la rotation a été effectuée,
> - Faux dans le cas contraire.
`<Image WDPic>` *Variable de type WDPic*
> Nom de la variable de type [WDPic](1000023503.md) contenant l'image à manipuler.
`<Angle>` *Réel*
> Angle de rotation (en degrés).
>
> - Si l'angle est positif, l'image est tournée dans le sens horaire.
> - Si l'angle est négatif, l'image est tournée dans le sens anti-horaire.
`<Options>` *Constante de type Entier*
> Options de rotation :
>
> | *drAdapte* | L'image est tournée avec un agrandissement. Elle est ensuite réduite pour correspondre à la taille initiale de l'image. |
> | --- | --- |
> | *drDéfaut* (valeur par défaut) | Agrandissement si nécessaire de l'image pour correspondre aux dimensions de l'image tournée. |
> | *drSansAgrandissement* | L'image est tournée mais n'est pas agrandie : l'image peut être tronquée. |

#### Effectuer une rotation en modifiant le centre de rotation

```wlangage
<Résultat> = PicRotation(<Image WDPic> , <Angle> , <X> , <Y> [, <Options>])
```

`<Résultat>` *Booléen*
> - Vrai si la rotation a été effectuée,
> - Faux dans le cas contraire.
`<Image WDPic>` *Variable de type WDPic*
> Nom de la variable de type [WDPic](1000023503.md) contenant l'image à manipuler.
`<Angle>` *Réel*
> Angle de rotation (en degrés).
>
> - Si l'angle est positif, l'image est tournée dans le sens horaire.
> - Si l'angle est négatif, l'image est tournée dans le sens anti-horaire.
`<X>` *Réel*
> Nouvelle abscisse du centre de rotation.
`<Y>` *Réel*
> Nouvelle ordonnée du centre de rotation.
`<Options>` *Constante optionnelle de type Entier*
> Options de rotation :
>
> | *drDéfaut* (Valeur par défaut) | Agrandissement si nécessaire de l'image pour correspondre aux dimensions de l'image tournée. |
> | --- | --- |
> | *drSansAgrandissement* | L'image est tournée mais n'est pas agrandie. |

## Liste des exemples associés :

| [image: Le type WDPIC] | Exemples unitaires (WINDEV) : Le type WDPIC     [ + ] Cet exemple montre l'utilisation du type WLangage WDPic. Ce type WLangage permet de modifier par programmation les images sources sauvegardées avec l'éditeur d'images. Il permet notamment de manipuler les calques. |
| --- | --- |

**Composante : wd300pic.dll**

## Voir Aussi

- [WDPic (Type de variable)](1000023503.md)
- [PicRedimensionne (Fonction)](1000024635.md)
- [PicSauve (Fonction)](1000023381.md)
- [Fonctions de gestion du champ Editeur d'images et des variables WDPic](1000023383.md)
- [<Variable WDPic>.Rotation (Fonction)](1000024922.md)
