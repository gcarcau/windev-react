---
title: "HRécupèreEnregistrement (Fonction)"
english: "HRetrieveRecord"
type: "Fonction"
id: "3044127"
module: "WDLang4"
category: "fonctions_hyper_file"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# HRécupèreEnregistrement (Fonction)

> **Disponible :** **WINDEV** (Windows, Linux, Java, Etats et Requêtes, Code Utilisateur (MCU)) · **WEBDEV** (Windows, Linux) · **WINDEV Mobile** (Android, Widget Android, iPhone/iPad) · **Autres** (Procédures stockées)

Renvoie le contenu de l'enregistrement en cours (dans un fichier de données, une vue HFSQL, une requête, ...).

## Exemple

```wlangage
Enreg est une chaîne = HRécupèreEnregistrement(Client)
```

```wlangage
// Récupère la chaîne contenant la totalité de l'enregistrement
MonEnregistrement est une chaîne = HRécupèreEnregistrement(Client)
// Extrait le nom du client
NomClient est une chaîne = ExtraitChaîne(MonEnregistrement, 1)
// Extrait le prénom du client
PrénomClient est une chaîne = ExtraitChaîne(MonEnregistrement, 2)
```

## Syntaxe

#### Récupérer le contenu d'un enregistrement pour le fichier spécifié

```wlangage
<Résultat> = HRécupèreEnregistrement([<Fichier de données> [, <Séparateur Rubrique> [, <Séparateur Tableau> [, <Format>]]]])
```

`<Résultat>` *Chaîne de caractères*
> Valeur de l'enregistrement, exprimée sous la forme :
>
>
> ```wlangage
> <Contenu rubrique 1> + <Séparateur rubrique> + <Contenu rubrique 2> + ...
> ```
>
>
>
> Les contenus des rubriques sont extraits selon l'ordre physique des rubriques dans le fichier de données. Si la rubrique correspond à une valeur NULL, <Contenu de la rubrique> correspond à une chaîne vide ("").
>
> Selon le <Format> précisé, cette chaîne de caractères est au format ANSI (par défaut) ou Unicode.
`<Fichier de données>` *Chaîne de caractères optionnelle*
> Nom du fichier de données (vue ou requête) manipulé. Si ce nom n'est pas spécifié, la fonction **HRécupèreEnregistrement** va manipuler le dernier fichier de données utilisé par la dernière fonction de gestion HFSQL (fonction commençant par la lettre H).
`<Séparateur Rubrique>` *Chaîne de caractères optionnelle*
> Chaîne de caractères utilisée pour séparer les différentes rubriques dans le <Résultat>. Par défaut, le séparateur utilisé est TAB.
`<Séparateur Tableau>` *Chaîne de caractères optionnelle*
> Chaîne de caractères utilisée pour séparer les valeurs des rubriques tableau (ou sous-rubriques) dans le <Résultat>. Par défaut, le séparateur utilisé est TAB.
> Attention : par compatibilité avec WINDEV 5.5, les séparateurs ne sont pas indiqués lorsque les rubriques tableau sont vides.
`<Format>` *Constante optionnelle de type Entier*
> Format utilisé pour renvoyer la valeur de l'enregistrement. Par défaut, une chaîne de caractères au format ANSI est renvoyé. Pour obtenir une chaîne de caractères au format Unicode, utilisez la constante suivante :
>
> | *hUnicode* | <Résultat est une chaîne de caractères Unicode. |
> | --- | --- |

> _(WINDEV, WEBDEV - Code Serveur)_ Récupérer le contenu d'un enregistrement pour la variable Enregistrement spécifiée Masquer les détails
<Résultat> = HRécupèreEnregistrement(<Enregistrement> [, <Séparateur Rubrique> [, <Séparateur Tableau> [, <Format>]]])

 <Résultat> : Chaîne de caractèresValeur de l'enregistrement, exprimée sous la forme :

<Contenu rubrique 1> + <Séparateur rubrique> + <Contenu rubrique 2> + ...

Les contenus des rubriques sont extraits selon l'ordre physique des rubriques dans le fichier de données. Si la rubrique correspond à une valeur NULL, <Contenu de la rubrique> correspond à une chaîne vide ("").Selon le <Format> précisé, cette chaîne de caractères est au format ANSI (par défaut) ou Unicode. <Enregistrement> : Variable optionnelle de type EnregistrementNom de la variable de type Enregistrement manipulée. <Séparateur Rubrique> : Chaîne de caractères optionnelleChaîne de caractères utilisée pour séparer les différentes rubriques dans le <Résultat>. Par défaut, le séparateur utilisé est TAB. <Séparateur Tableau> : Chaîne de caractères optionnelleChaîne de caractères utilisée pour séparer les valeurs des rubriques tableau (ou sous-rubriques) dans le <Résultat>. Par défaut, le séparateur utilisé est TAB.Attention : par compatibilité avec WINDEV 5.5, les séparateurs ne sont pas indiqués lorsque les rubriques tableau sont vides. <Format> : Constante optionnelle de type EntierFormat utilisé pour renvoyer la valeur de l'enregistrement. Par défaut, une chaîne de caractères au format ANSI est renvoyé. Pour obtenir une chaîne de caractères au format Unicode, utilisez la constante suivante :hUnicode <Résultat> est une chaîne de caractères Unicode.

## Remarques

- Le contenu des rubriques de type mémo binaire n'est pas récupéré, mais le séparateur de rubrique est présent (pour éviter un décalage).
- Le contenu des clés composées n'est pas récupéré, mais le séparateur de rubrique est présent (pour éviter un décalage).

**Classification Métier / UI : Code métier**

**Composante : wd300hf.dll**

## Voir Aussi

- [HListeRubrique (Fonction)](3044230.md)
- [HRécupèreRubrique (Fonction)](3044128.md)
- [HVersRubrique (Fonction)](3044025.md)
- [Fonctions de gestion HFSQL](3044156.md)
- [<Source>.RécupèreEnregistrement (Fonction)](1000025057.md)
