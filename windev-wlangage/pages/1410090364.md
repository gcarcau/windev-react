---
title: "<Variable Drive>.RécupèreRépertoire (Fonction)"
english: "<Drive variable>.DownloadDirectory"
type: "Fonction"
id: "1410090364"
module: "WDLang3"
category: "fonctions_drive"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# <Variable Drive>.RécupèreRépertoire (Fonction)

> **Disponible :** **WINDEV** (Windows, Etats et Requêtes, Code Utilisateur (MCU)) · **WEBDEV** (Windows, Linux) · **WINDEV Mobile** (Android, iPhone/iPad)

Récupère un répertoire (et ses fichiers) présent sur un drive (disque distant) pour l'enregistrer sur le poste de l'utilisateur.

## Exemple

```wlangage
oOAuth2Param est un OAuth2Paramètres
oOAuth2Param.ClientID = "client_id"
oOAuth2Param.ClientSecret = "client_secret"
oOAuth2Param.URLRedirection = "http://localhost:9000/"

oDrive est un Drive
oDrive = oDriveConnecte(oOAuth2Param)

// Choix du répertoire local
sRépertoire est une chaîne
sRépertoire = fRepSélecteur(fRepExe(), "Répertoire local où récupérer le répertoire")

// Récupère le répertoire
SI oDrive.RécupèreRépertoire("/MesPhotos", sRépertoire) = Faux ALORS
	Erreur("Impossible de récupérer le répertoire demandé", ErreurInfo())
	RETOUR
FIN
```

## Syntaxe

```wlangage
<Résultat> = <Drive>.RécupèreRépertoire(<Répertoire à récupérer> , <Répertoire local>)
```

`<Résultat>` *Booléen*
> - Vrai si le répertoire et ses fichiers ont été récupérés,
> - Faux dans le cas contraire. Pour connaître le libellé de l'erreur, utilisez la fonction [ErreurInfo](3013008.md) avec la constante *errMessage*.
`<Drive>` *Variable de type Drive ou gglDrive*
> Nom de la variable de type [Drive](1410089369.md) ou [gglDrive](1410089374.md) correspondant au drive à manipuler. La connexion au drive voulu doit avoir été effectuée. Dans le cas contraire, le répertoire n'est pas récupéré.
`<Répertoire à récupérer>` *Chaîne de caractères*
> Chemin absolu du répertoire présent sur le poste distant et devant être récupéré. Si ce paramètre ne correspond pas à un chemin valide, le répertoire ne sera pas récupéré.
`<Répertoire local>` *Chaîne de caractères*
> Chemin absolu du répertoire à enregistrer sur le poste local. Si ce paramètre ne correspond pas à un chemin valide ou si un répertoire de même nom existe déjà, le répertoire ne sera pas récupéré.

## Remarques

Le répertoire récupéré et ses fichiers n'ont plus de lien avec le drive. Le répertoire et les fichiers récupérés sur le poste de l'utilisateur ne seront pas modifiés si le répertoire et les fichiers présents sur le drive sont modifiés (et inversement).

**Classification Métier / UI : Code métier**

**Composante : wd300ggl.dll**

## Voir Aussi

- [<Variable Drive>.RécupèreFichier (Fonction)](1410090362.md)
- [<Variable gglDrive>.RécupèreFichierGgl (Fonction)](1410090363.md)
- [Fonctions de gestion des drives : OneDrive, Google Drive, Dropbox (syntaxe préfixée)](1410090371.md)
- [Manipuler des fichiers sur Dropbox, Google Drive et OneDrive](1410090345.md)
- [DriveRécupèreRépertoire (Fonction)](1410089559.md)
