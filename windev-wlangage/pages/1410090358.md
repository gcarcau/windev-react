---
title: "<Variable Drive>.EnvoieRépertoire (Fonction)"
english: "<Drive variable>.UploadDirectory"
type: "Fonction"
id: "1410090358"
module: "WDLang3"
category: "fonctions_drive"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# <Variable Drive>.EnvoieRépertoire (Fonction)

> **Disponible :** **WINDEV** (Windows, Etats et Requêtes, Code Utilisateur (MCU)) · **WEBDEV** (Windows, Linux) · **WINDEV Mobile** (Android, iPhone/iPad)

Envoie un répertoire et ses fichiers (présents sur la machine de l'utilisateur) vers un drive (disque distant).

## Exemple

```wlangage

oOAuth2Param est un OAuth2Paramètres
oOAuth2Param.ClientID = "client_id"
oOAuth2Param.ClientSecret = "client_secret"
oOAuth2Param.URLRedirection = "http://localhost:9000/"
// Connexion au drive OneDrive

oDrive est un Drive
oDrive = oDriveConnecte(oOAuth2Param)

// Sélection et envoi du répertoire
sRépertoire est une chaîne
sRépertoire = fRepSélecteur(fRepExe(), "Répertoire local à envoyer")
SI oDrive.EnvoieRépertoire(sRépertoire, "/A vérifier") = Faux ALORS
 Erreur("Impossible d'envoyer le répertoire demandé", ErreurInfo())
 RETOUR
FIN
```

## Syntaxe

```wlangage
<Résultat> = <Drive>.EnvoieRépertoire(<Répertoire à envoyer> , <Répertoire destination>)
```

`<Résultat>` *Booléen*
> - Vrai si le répertoire a été envoyé,
> - Faux dans le cas contraire. Pour connaître le libellé de l'erreur, utilisez la fonction [ErreurInfo](3013008.md) avec la constante *errMessage*.
`<Drive>` *Variable de type Drive ou gglDrive*
> Nom de la variable de type [Drive](1410089369.md) ou [gglDrive](1410089374.md) correspondant au drive à manipuler. La connexion au drive voulu doit avoir été effectuée. Dans le cas contraire, le répertoire n'est pas envoyé.
`<Répertoire à envoyer>` *Chaîne de caractères*
> Chemin absolu du répertoire présent sur le poste local et devant être envoyé. Si ce paramètre ne correspond pas à un chemin valide, le répertoire et son contenu ne seront pas envoyés.
`<Répertoire destination>` *Chaîne de caractères*
> Chemin absolu du répertoire destination dans le drive.
>
> - Si ce paramètre ne correspond pas à un chemin valide, l'élément ne sera pas envoyé.
> - Les répertoires manquants dans le chemin ne sont pas créés.
> - Si le répertoire existe déjà à l'emplacement indiqué dans le drive, il sera fusionné avec le répertoire envoyé.

## Remarques

### Dropbox
Selon le type de compte Dropbox utilisé, les appels à cette fonction peuvent être considérés comme des transferts de données. Certains types de compte ont une limite pour les données transférées par mois. Pour plus de détails, consultez [https://www.dropbox.com/developers/reference/data-transport-limit](https://www.dropbox.com/developers/reference/data-transport-limit).

### Paramètre <Répertoire destination>
Exemples de chemins valides :

```wlangage
"/mon répertoire de vacances/2021/""\photos"
```

Exemples de chemins invalides :

```wlangage
"répertoireA/RépertoireB""Ce/Chemin/Est/invalide""c:\Users\"
```

**Classification Métier / UI : Code métier**

**Composante : wd300ggl.dll**

## Voir Aussi

- [Fonctions de gestion des drives : OneDrive, Google Drive, Dropbox (syntaxe préfixée)](1410090371.md)
- [<Variable Drive>.EnvoieFichier (Fonction)](1410090357.md)
- [Manipuler des fichiers sur Dropbox, Google Drive et OneDrive](1410090345.md)
- [DriveEnvoieRépertoire (Fonction)](1410089564.md)
