---
title: "FichierVersTableMémoire (Fonction)"
english: "FileToMemoryTable"
type: "Fonction"
id: "3074005"
module: "WDLang1"
category: "fonctions_table"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# FichierVersTableMémoire (Fonction)

> **Disponible :** **WINDEV** (Windows, Linux, Etats et Requêtes, Code Utilisateur (MCU)) · **WEBDEV** (Windows, Linux, PHP)

Remplit un champ de type "Table par programmation" (également appelé "Table mémoire") avec tous les enregistrements d'un fichier de données, d'une vue HFSQL ou d'une requête (requête créée avec l'éditeur de requêtes ou avec la fonction [HExécuteRequêteSQL](3044084.md)).
Le champ Table par programmation doit contenir les différentes colonnes à associer aux rubriques. La fonction **FichierVersTableMémoire** va :

- vider le champ Table.
- associer les colonnes du champ Table aux rubriques du fichier de données, d'une vue HFSQL ou d'une requête. Les rubriques vont être associées selon leur ordre physique défini dans le fichier de données.
- remplir le champ Table avec le contenu du fichier de données ou de la requête.

**ATTENTION** : Cette opération peut être très longue et saturer la mémoire si le fichier de données est volumineux.

## Exemple

```wlangage
// Remplir le champ TABLE_MaTable avec le contenu de REQ_Requête1
FichierVersTableMémoire(TABLE_MaTable, REQ_Requête1)
```

## Syntaxe

```wlangage
FichierVersTableMémoire(<Champ Table> , <Fichier de données>)
```

`<Champ Table>` *Nom de champ*
> Nom du champ Table par programmation à remplir.
`<Fichier de données>` *Chaîne de caractères*
> Nom du fichier de données, de la vue HFSQL ou de la requête utilisé.

## Remarques

### Les différents types de requêtes
La fonction **FichierVersTableMémoire** peut être utilisée avec les requêtes créées sous l'éditeur de requêtes ou avec la fonction [HExécuteRequêteSQL](3044084.md).
Pour associer une requête créée avec la fonction [SQLExec](3072007.md) à un champ Table par programmation, utilisez la fonction [SQLTable](3072031.md).

### Parcours du fichier de données
Le parcours du fichier de données se fait sur le contenu de la propriété [RubriqueParcourue](2510111.md).
Pour que la fonction **FichierVersTableMémoire** prenne en compte le filtre appliqué au fichier de données, il faut :

1. Modifier la propriété [RubriqueParcourue](2510111.md) en lui affectant la rubrique renvoyée par la fonction [HFiltre](3044100.md).
2. Utiliser la fonction **FichierVersTableMémoire**.

### Fonction FichierVersTableMémoire et propriété Nouveau
La fonction **FichierVersTableMémoire** affecte la propriété [Nouveau](2510073.md) à Vrai.

> _(WINDEV)_ Versions précédentesCette fonction remplace la fonction HVueVersTable, conservée par compatibilité avec WINDEV 5.5.

**Composante : wd300obj.dll**

## Voir Aussi

- [TableAjouteLigne (Fonction)](3074006.md)
- [Fonctions de gestion des champs Table](3074009.md)
- [Comment remplir un champ Table par programmation ?](20025.md)
