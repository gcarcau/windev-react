---
title: "Manipuler un champ Liste image par programmation (syntaxe préfixée)"
type: "syntaxe préfixée"
id: "1013314"
module: "WDControls"
category: "champ_liste_image"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# Manipuler un champ Liste image par programmation (syntaxe préfixée)

> **Disponible :** **WINDEV** (Windows, Java)

## Présentation

WINDEV permet de manipuler un champ Liste image directement par programmation. Il suffit d'utiliser directement la variable du champ Liste image dans le code. Cette variable est de type numérique.
La variable du champ Liste image :

- correspond au nom du champ Liste image.
- est initialisée avec l'indice de l'élément sélectionné dans le champ Liste image.

**Remarque** : Pour manipuler un élément d'un champ Liste image auquel aucune rubrique mémorisée n'est associée, il suffit d'utiliser la notation suivante :

```wlangage
<Champ Liste image> [ <Indice de l'élément>]
```

## Initialiser un champ Liste image

### Initialiser un élément
Pour initialiser un élément d'un champ Liste image, utilisez la fonction [<Liste>.Ajoute](1000017319.md).

```wlangage
LSI_ListeImage1.Ajoute("Soleil", "C:\IMG\Soleil.JPG")
LSI_ListeImage1.Ajoute(Photo)
```

### Remplir un champ Liste image
Pour initialiser un champ Liste image, il suffit d'utiliser une boucle d'itération pour initialiser tous les éléments du champ.

```wlangage
Indice est un entierPOUR Indice = <Position Début> A <Position Fin> <Champ Liste image>.Ajoute(<Valeur>, <Image>)FIN
```

```wlangage
i est un entier
POUR i = 1 À 52
	LSI_ListeImage1.Ajoute(NOM[i], Image[i])
FIN
```

Nouveauté 2025

> _(WINDEV)_ La fonction <Liste image>.ProcédureChargeImage permet de définir une procédure WLangage qui sera appelée lors de l'affichage des lignes de la liste. Cette procédure permet d'utiliser des images dynamiques (et non obligatoirement stockées sur le disque).

## Sélectionner un élément dans un champ Liste image

### Sélectionner l'élément de la ligne <Indice>
Pour sélectionner l'élément de la ligne <Indice>, utilisez :

- soit l'affectation directe :
  ```wlangage
  <Champ Liste image> = Indice
  ```
- soit la fonction [<Liste>.SelectPlus](1000017414.md) :
  ```wlangage
  <Champ Liste image>.SelectPlus(<Indice>)
  ```

### Se positionner sur le dernier élément du champ Liste image
Pour se positionner sur le dernier élément du champ Liste image, utilisez :

- soit la propriété [Occurrence](3025009.md) :
  ```wlangage
  <Champ Liste image>.Occurrence
  ```
- soit la fonction [<Liste>.SelectPlus](1000017414.md) associée à la propriété [Occurrence](3025009.md) :
  ```wlangage
  <Champ Liste image>.SelectPlus(<Champ Liste image>.Occurrence)
  ```
- soit la fonction [<Liste>.Occurrence](1000017334.md) :
  ```wlangage
  <Champ Liste image>.Occurrence()
  ```

## Récupérer l'élément sélectionné dans le champ Liste image

### Récupérer l'indice de l'élément sélectionné
Pour récupérer l'indice de l'élément sélectionné, utilisez :

- soit la lecture directe de l'élément :
  ```wlangage
  Indice = <Champ Liste image>
  ```
- soit la fonction [<Liste>.Select](1000017336.md) :
  ```wlangage
  Indice = <Champ Liste image>.Select()
  ```

**Remarque** : Pour les champs Liste image multisélection, la fonction [<Liste>.Select](1000017336.md) permet de récupérer les éléments sélectionnés.

```wlangage
// Récupérer les éléments d'un champ Liste image multisélection
Rang est un entier = 0  // numéro de la sélection
Indice est un entier = 0  // indice de l'élément sélectionné
BOUCLE
	Rang = Rang + 1
	Indice = LSI_ListeImage1.Select(Rang)
	SI Indice = -1 ALORS SORTIR
FIN
```

### Récupérer la valeur de l'élément sélectionné
Pour récupérer la valeur de l'élément sélectionné, utilisez une des syntaxes suivants :

- Syntaxe 1 :
  ```wlangage
  Indice est un entier = <Champ Liste image><Variable> = <Champ Liste image>[Indice]
  ```
- Syntaxe 2 :
  ```wlangage
  <Variable> = <Champ Liste image>[<Champ Liste image>]
  ```
- Syntaxe 3 : Propriété [ValeurAffichée](2510129.md)
  ```wlangage
  <Variable> = <Champ Liste image>.ValeurAffichée
  ```
  **Attention** : La valeur mémorisée en programmation peut être différente de la valeur affichée. Pour connaître la valeur mémorisée correspondant à une des lignes du champ Liste image, utilisez la propriété [ValeurMémorisée](2510127.md).

## Propriétés spécifiques à la gestion des champs Liste image

Les propriétés suivantes sont spécifiques à la gestion des champs Liste image :

| **Tout type de champ Liste image** |  |
| --- | --- |
| [Mémoire](2510076.md) | Permet de savoir si le champ spécifié est un champ Liste image par programmation ou Liste image fichier. |
| [ModeListeImage](2510079.md) | Change le mode d'affichage du champ Liste image (mode liste image, mode liste classique, mode panorama). |
| [Occurrence](3025009.md) | Permet de connaître le nombre de lignes présentes dans un champ Liste image. |
| [OrientationVerticale](2510092.md) | Permet de connaître et de modifier l'orientation d'un champ Liste image. |
| [TypeRemplissage](1000017723.md) | Permet de connaître le type de remplissage (fichier, par programmation, sur variable) d'un champ Liste image. |
| [ValeurAffichée](2510129.md) | Permet de connaître la valeur de la ligne en cours ou d'une ligne spécifique du champ Liste image. |
| [Vide](2510126.md) | Permet de savoir si un champ Liste image est vide. |
| **Champ Liste image par programmation uniquement** |  |
| [Triée](2510115.md) | Permet de savoir si un champ Liste image par programmation est triée et de trier un champ Liste image par programmation (tri réalisé en fonction des libellés des éléments). |
| **Champ Liste image Fichier uniquement** |  |
| [FichierParcouru](2510040.md) | Permet de connaître et de modifier le fichier de données ou la requête utilisé pour l'affichage des enregistrements dans les champs Liste image fichier. |
| [ParcoursAuto](2510093.md) | Permet de savoir si le parcours d'un champ Liste image fichier est automatique ou programmé. |
| [RubriqueAffichée](2510123.md) | Permet de connaître ou de modifier la rubrique correspondant au libellé affiché dans un champ Liste image. |
| [RubriqueAffichéeImage](2510109.md) | Permet de connaître ou de modifier la rubrique correspondant à l'image affichée dans un champ Liste image. |
| [RubriqueMémorisée](2510110.md) | Permet de connaître ou de modifier la rubrique mémorisée d'un champ Liste image. |
| [RubriqueParcourue](2510111.md) | Permet de connaître et de modifier la rubrique utilisée pour le parcours automatique des champs Liste image. |

Pour connaître la liste complète des propriétés du WLangage pouvant être utilisées avec un champ de type Liste image, consultez [Propriétés associées au champ Liste image](1013219.md).

## Voir Aussi

- [Le champ Liste image](1013186.md)
- [Manipuler un champ Liste image par programmation](1013198.md)
