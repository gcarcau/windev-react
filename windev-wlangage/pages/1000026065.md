---
title: "<modbusSession>.LitBit (Fonction)"
english: "<modbusSession>.ReadBit"
type: "Fonction"
id: "1000026065"
module: "WDLang3"
category: "fonctions_modbus"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# <modbusSession>.LitBit (Fonction)

> **Disponible :** **WINDEV** (Windows, Linux, Etats et Requêtes, Code Utilisateur (MCU)) · **WEBDEV** (Windows, Linux)

Lit un ou plusieurs bits à partir d'une adresse précise sur un esclave Modbus.

## Exemple

```wlangage
// Crée une session et connexion à un serveur Modbus
Session est un modbusSession
Session = ModbusConnecteTCP("192.168.1.7")

tabRéponse est un tableau de 5 entiers

// Si la connexion échoue, affiche l'erreur
SI ErreurDétectée = Vrai ALORS
	Trace(ErreurInfo())
SINON
	// Lit une séquence de 5 bits en lecture-écriture à partir de l'adresse 0x42
	tabRéponse = Session.LitBit(0x42, 5, Faux)
	// Si il y a eu une erreur, affiche l'erreur
	SI ErreurDétectée = Vrai ALORS
		Trace(ErreurInfo())
	FIN
FIN

// Déconnexion
Session.Déconnecte()
```

## Syntaxe

#### Lire 1 bit dans un esclave Mobdus

```wlangage
<Résultat> = <Session>.LitBit(<Adresse> , <Lecture seule>)
```

`<Résultat>` *Booléen*
> Valeur du bit lue (Vrai ou Faux).
`<Session>` *Variable de type modbusSession*
> Nom de la variable de type [modbusSession](1000025924.md) correspondant à la session de communication vers l'esclave Modbus.
`<Adresse>` *Entier*
> Adresse de lecture du bit dans l'esclave (comprise entre 0 et 65 535).
`<Lecture seule>` *Booléen*
> - Vrai si les bits à lire font partie des données en lecture seule (correspond au code fonction Modbus 2 (Read Discrete Inputs)).
> - Faux si les bits à lire font partie des données en lecture-écriture (correspond au code fonction Modbus 1 (Read Coils)).

#### Lire plusieurs bits dans un esclave Mobdus

```wlangage
<Résultat> = <Session>.LitBit(<Adresse> , <Nombre de bits> , <Lecture seule>)
```

`<Résultat>` *Tableau*
> Tableau d'entiers contenant la valeur des bits lus (Vrai ou Faux).
`<Session>` *Variable de type modbusSession*
> Nom de la variable de type [modbusSession](1000025924.md) correspondant à la session de communication vers l'esclave Modbus.
`<Adresse>` *Entier*
> Début de l'adresse de lecture des bits dans l'esclave (comprise entre 0 et 65 535).
`<Nombre de bits>` *Entier*
> Nombre de bits à lire (que l'esclave doit envoyer).
`<Lecture seule>` *Booléen*
> - Vrai si les bits à lire font partie des données en lecture seule (correspond au code fonction Modbus 2 (Read Discrete Inputs)).
> - Faux si les bits à lire font partie des données en lecture-écriture (correspond au code fonction Modbus 1 (Read Coils)).

## Remarques

- L'exécution de cette fonction est bloquante tant que l'esclave n'a pas répondu au message ou tant que le délai d'attente de la session n'est pas dépassé.
- Après l'appel de la fonction **<modbusSession>.LitBit**, il est conseillé de vérifier la variable [ErreurDétectée](3087001.md) pour s'assurer qu'il n'y a pas d'erreur.

**Classification Métier / UI : Code métier**

**Composante : wd300com.dll**

## Voir Aussi

- [Fonctions Modbus (syntaxe préfixée)](1000026091.md)
- [<modbusSession>.EcritBit (Fonction)](1000025967.md)
- [ModbusLitBit (Fonction)](1000025935.md)
- [Modbus](1000026101.md)
