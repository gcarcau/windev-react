---
title: "Opérateur de conversion explicite sur les types avancés"
id: "1515005"
module: "Keywords"
category: "operateurs"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# Opérateur de conversion explicite sur les types avancés

> **Disponible :** **WINDEV** (Windows, Linux, Java, Code Utilisateur (MCU)) · **WEBDEV** (Windows, Linux) · **WINDEV Mobile** (Android, Widget Android, iPhone/iPad, Widget IOS, Apple Watch, Mac Catalyst)

Les instances d'une variable de type avancé qui héritent d'un type avancé de base peuvent être manipulées par une variable du type de base.
Pour manipuler l'instance par son type réel à partir d'une variable du type de base, il est possible d'utiliser une variable du type réel et l'affecter grâce à l'opérateur <-.

L'opérateur de conversion explicite permet de manipuler l'instance par son type réel sans variable intermédiaire.

## Exemple

```wlangage
// Code n'utilisant pas la conversion explicite
POUR TOUTE formeDiag DE EDIAG_MonDiagramme.Forme
	FormeImage est un diagImage <- formeDiag
	SI FormeImage <> Null ALORS
 	 // La forme est une image
	FIN
FIN
```

```wlangage
// Code utilisant la conversion explicite
POUR TOUTE formeDiag DE EDIAG_MonDiagramme.Forme
	SI ((diagImage?)formeDiag) ALORS
 	 // La forme est une image
	FIN
FIN
```

## Syntaxe

#### Conversion statique : Provoque une exception en exécution si la conversion est illicite

```wlangage
((<Type dérivé>) <Variable du type de base>)
```

`<Type dérivé>` **
> Type de variable héritant du type de base.
`<Variable du type de base>` **
> Variable à convertir vers le <Type dérivé>.

#### Conversion dynamique : Renvoie Null si la conversion est illicite

```wlangage
((<Type dérivé> ?) <Variable du type de base>)
```

`<Type dérivé>` **
> Type de variable héritant du type de base.
`<Variable du type de base>` **
> Variable à convertir vers le <Type dérivé>.
**Détail** : La conversion dynamique ne provoque pas d'erreur en exécution si la conversion est illicite. Un objet fictif est renvoyé pour lequel la comparaison à **Null** renvoie Vrai. Une exception sera déclenchée si cet objet fictif est manipulé.

## Voir Aussi

- [Types avancés](1514041.md)
