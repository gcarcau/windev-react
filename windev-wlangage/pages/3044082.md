---
title: "HErreurMotDePasse (Fonction)"
english: "HErrorPassword"
type: "Fonction"
id: "3044082"
module: "WDLang4"
category: "fonctions_hyper_file"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# HErreurMotDePasse (Fonction)

> **Disponible :** **WINDEV** (Windows, Linux, Java, Etats et Requêtes, Code Utilisateur (MCU)) · **WEBDEV** (Windows, Linux) · **WINDEV Mobile** (Android, Widget Android, iPhone/iPad, Widget IOS) · **Autres** (Procédures stockées)

Après l'exécution d'une fonction de lecture ou d'écriture dans un fichier de données, permet de savoir si une erreur due à un mot de passe erroné a été rencontrée sur ce fichier de données.
En général, il est conseillé d'utiliser la fonction **HErreurMotDePasse** après la première fonction manipulant un fichier de données HFSQL.

Cette fonction peut être utilisée indifféremment avec les fichiers de données HFSQL, les vues HFSQL ou les requêtes.

> _(WINDEV)_ Par défaut, WINDEV propose une gestion automatique des erreurs de mots de passe. Cette fonctionnalité n'est pas disponible dans le code des procédures stockées.

> _(Java)_ Seules les erreurs de mot de passe survenues sur des fichiers de données HFSQL sont signalées.

## Exemple

```wlangage
// Vérification du compte-rendu de la fonction HLitPremier
SI HLitPremier(Client, Nom) = Faux ALORS
	// Problème lors de la lecture
	// Vérifie si l'erreur est due à un mot de passe erroné
	SI HErreurMotDePasse() = Vrai ALORS
		Info("Le mot de passe donné pour manipuler le fichier de données Client est faux")
		 // Ouverture de la fenêtre de saisie du mot de passe
		MotDePasse = Ouvre(FEN_Saisie_Passe)
		HPasse(Client, MotDePasse)
	FIN
FIN
```

## Syntaxe

```wlangage
<Résultat> = HErreurMotDePasse()
```

`<Résultat>` *Booléen*
> - Vrai en cas de mot de passe erroné,
> - Faux si le mot de passe est correct.

**Classification Métier / UI : Code métier**

**Composante : wd300hf.dll**

## Voir Aussi

- [Fonctions de gestion HFSQL](3044156.md)
