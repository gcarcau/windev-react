---
title: "Instruction BOUCLE"
english: "LOOP"
id: "1510004"
module: "Keywords"
category: "instructions_struc"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# Instruction BOUCLE

> **Disponible :** **WINDEV** (Windows, Linux, Java, Etats et Requêtes, Code Utilisateur (MCU)) · **WEBDEV** (Windows, Linux, WEBDEV - Code Navigateur) · **WINDEV Mobile** (Android, Widget Android, iPhone/iPad, Widget IOS, Apple Watch, Mac Catalyst)

Le bloc d'instructions est répété à l'infini. Le nombre d'itérations du bloc d'instructions n'est pas contrôlé, il n'y a pas d'expression à évaluer.

## Exemple

```wlangage
// Sortie selon une condition SI
BOUCLE
	// Lecture d'une ligne du fichier texte
	UneLigne = fLitLigne(NumFic)
	 SI UneLigne = EOT ALORS SORTIR
	TraiteLigne(UneLigne)
FIN
```

```wlangage
// Sortie selon une condition TANTQUE
BOUCLE
	// Lecture d'une ligne du fichier texte
	UneLigne = fLitLigne(NumFic)
	TraiteLigne(UneLigne)
À FAIRE TANTQUE UneLigne <> EOT
```

```wlangage
// Sortie selon une itération
BOUCLE (10)
	// Lecture d'une ligne du fichier texte
	UneLigne = fLitLigne(NumFic)
	TraiteLigne(UneLigne)
FIN
```

## Syntaxe

#### Boucle avec sortie selon une condition "SI"

```wlangage
BOUCLE ... SI <Condition> ALORS SORTIR ...FIN
```

`<BOUCLE>` **
> Signale le début du bloc d'instructions.
`<SORTIR>` **
> Permet de quitter le bloc d'instructions.
`<FIN>` **
> Signale la fin du bloc d'instructions.

#### Boucle avec sortie selon une condition "TANTQUE"

```wlangage
BOUCLE...A FAIRE TANTQUE <Condition>
```

`<BOUCLE>` **
> Signale le début du bloc d'instructions.
`<A FAIRE TANTQUE>` **
> Signale la fin du bloc d'instructions. Permet de quitter le bloc d'instructions. Les lignes de la boucle situées avant cette instruction sont exécutées.

#### Boucle avec sortie selon le nombre d'itérations

```wlangage
BOUCLE (<Nombre d'itérations>)...FIN
```

`<BOUCLE>` **
> Signale le début du bloc d'instructions.
`<Nombre d'itérations>` **
> Nombre d'itérations à exécuter. La sortie de la boucle se fera lorsque le nombre d'itérations sera atteint.
`<FIN>` **
> Signale la fin du bloc d'instructions.

## Remarques

### Code à exécuter
Le code à exécuter est placé entre les instructions **BOUCLE** et **FIN**.

### Sortir d'une boucle
Plusieurs instructions sont possibles :

- [RETOUR](1510009.md) : Sortie de la boucle et sortie du traitement (ou de la procédure) en cours.
- [RENVOYER](1510007.md) : Renvoi d'un compte-rendu au traitement appelant. Sortie de la boucle et sortie du traitement (ou de la procédure) en cours.
- [SORTIR](1510012.md) : Sortie de la boucle et exécution de la suite du traitement en cours.

La fonction [Ferme](3038018.md) permet de sortir de la boucle et de fermer la fenêtre en cours.

**Attention** : Il n'est pas possible d'utiliser les instructions [RETOUR](1510009.md) et [RENVOYER](1510007.md) **dans un même traitement**.

### Exécution de l'itération suivante
Pour exécuter directement l'itération suivante **sans terminer le code de l'itération en cours**, utilisez l'instruction [CONTINUER](1510003.md) :
```wlangage
BOUCLE ... SI <Condition> ALORS CONTINUER  // Retour au mot-clé BOUCLE ...FIN
```

### Boucle sans fin
Lors de la compilation du projet, une boucle sans fin évidente (absence des instructions [SORTIR](1510012.md), [RETOUR](1510009.md) ou [RENVOYER](1510007.md)) est signalée par un warning.

### Variable externe dans une boucle
Une variable déclarée avec le mot-clé [EXTERNE](1511013.md) ne peut pas être utilisée dans une boucle.

## Voir Aussi

- [Instructions composées](1510006.md)
- [Instructions simples](1510002.md)
- [Instruction POUR](1510001.md)
- [Instruction TANTQUE](1510010.md)
