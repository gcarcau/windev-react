---
title: "UTILISER DANS (Section critique)"
english: "USING IN"
id: "1510025"
module: "Keywords"
category: "instructions_struc"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# UTILISER DANS (Section critique)

> **Disponible :** **WINDEV** (Windows, Linux, Etats et Requêtes, Code Utilisateur (MCU)) · **WEBDEV** (Windows, Linux, PHP, WEBDEV - Code Navigateur) · **WINDEV Mobile** (iPhone/iPad, Widget IOS, Apple Watch, Mac Catalyst)

L'instruction **UTILISER ... DANS** permet d'effectuer automatiquement les opérations d'initialisation et de terminaison sur les sections critiques.
Cette instruction permet principalement "d'oublier ces opérations" (notamment les opérations de terminaison) et de rendre le code plus lisible par la mise en évidence du bloc sur lequel porte la fonctionnalité.

Remarque : En cas de sortie de l'instruction **UTILISER ... DANS** via une instruction RENVOYER ou RETOUR, la section est alors automatiquement terminée.

## Exemple

```wlangage
// Utilisation d'une variable de type Section Critique
Section est un SectionCritique

// Utilisation pour un bloc de code
//---------------------------
UTILISER Section DANS
	// code protégé par la section critique
	// un seul thread pourra exécuter ce code à un moment donné
FIN
```

```wlangage
// Utilisation d'une section critique nommée
UTILISER SectionCritique("MaSection") DANS
	// Deux threads ne pourront pas exécuter ce code en même temps
	...
FIN
```

```wlangage
// Section critique sur une variable
t est un tableau <section critique> d'entiers

UTILISER SectionCritique(t) DANS
	// Manipulations sur le tableau par un seul thread en même temps
	...
FIN
```

## Syntaxe

#### Définir une section critique à l'aide d'une variable SectionCritique

```wlangage
UTILISER <Section Critique> DANS ...FIN
```

`<UTILISER DANS>` **
> Signale le début du bloc d'instructions.
`<Section Critique>` *Variable de type SectionCritique*
> Nom de la variable de type [SectionCritique](1000023972.md) permettant d'identifier la section critique.

#### Définir une section globale

```wlangage
UTILISER SectionCritique() DANS ...FIN
```

`<UTILISER DANS>` **
> Signale le début du bloc d'instructions.
`<SectionCritique>` **
> Active la section critique. Pour plus de détails, consultez [SectionCritique](1000024373.md).

#### Définir une section critique nommée

```wlangage
UTILISER SectionCritique(<Nom de la section>) DANS ...FIN
```

`<UTILISER DANS>` **
> Signale le début du bloc d'instructions.
`<SectionCritique>` **
> Active la section critique. Pour plus de détails, consultez [SectionCritique](1000024373.md).
`<Nom de la section>` *Chaîne de caractères*
> Identifie la section critique. Ce paramètre permet de distinguer les sections de codes protégées.

#### Définir une section critique sur une variable avec l'attribut d'extension "Section critique"

```wlangage
UTILISER SectionCritique(<Variable>) DANS ...FIN
```

`<UTILISER DANS>` **
> Signale le début du bloc d'instructions.
`<SectionCritique>` **
> Active la section critique. Pour plus de détails, consultez [SectionCritique](1000024373.md).
`<Variable>` *Tout type*
> Nom de la variable WLangage à protéger dans une section critique. Cette variable doit être définie avec l'attribut d'extension <section critique>.

## Voir Aussi

- [Gérer les sections critiques](1000021292.md)
- [SectionCritique (Fonction)](1000024373.md)
- [SectionCritiqueDébut (Fonction)](3077001.md)
- [SectionCritiqueFin (Fonction)](3077005.md)
- [SectionCritique (Type de variable)](1000023972.md)
- [Fonctions de gestion des threads](3077003.md)
- [UTILISER DANS (Chronomètre)](1510026.md)
