---
title: "HMiseAJourRequête (Fonction)"
english: "HUpdateQuery"
type: "Fonction"
id: "3044365"
module: "WDLang4"
category: "fonctions_cs"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# HMiseAJourRequête (Fonction)

> **Disponible :** **WINDEV** (Windows, Linux, Java, Etats et Requêtes, Code Utilisateur (MCU)) · **WEBDEV** (Windows, Linux) · **WINDEV Mobile** (Android, Widget Android, iPhone/iPad, Widget IOS)

Crée ou met à jour une requête sur un serveur HFSQL. La requête (fichier .WDR) sera alors utilisable dans une procédure stockée sur le serveur.

## Exemple

```wlangage
// Description de la connexion
MaConnexion est une Connexion

// Mettre à jour ou créer la requête
HMiseAJourRequête(MaConnexion, Requête1)

// Mettre à jour la requête même si le serveur possède une version plus récente de la requête
HMiseAJourRequête(MaConnexion, Requête1, Vrai)
```

## Syntaxe

```wlangage
<Résultat> = HMiseAJourRequête(<Connexion> , <Requête> [, <Forcer>])
```

`<Résultat>` *Booléen*
> - Vrai si la requête a été créée ou mise à jour,
> - Faux dans le cas contraire. La fonction [HErreur](3044088.md) permet d'identifier l'erreur.
`<Connexion>` *Chaîne de caractères ou variable de type Connexion*
> Connexion vers le serveur sur lequel la requête doit être créée ou mise à jour. Cette connexion correspond :
>
> - soit au nom de la connexion définie sous l'éditeur d'analyses ou grâce aux fonctions [HDécritConnexion](3044205.md) ou [HOuvreConnexion](3044107.md).
> - soit au nom d'une variable de type [Connexion](1514073.md).
`<Requête>` *Chaîne de caractères*
> Nom de la requête à créer ou à mettre à jour sur le serveur.
> Si ce paramètre correspond à "*", toutes les requêtes seront mises à jour ou créées sur le serveur.
`<Forcer>` *Booléen optionnel*
> - Faux (par défaut) : la requête sera créée ou mise à jour :
>   - si le serveur ne possède pas la requête.
>   - si le serveur possède une version plus ancienne de la requête.
> - Vrai : la requête sera mise à jour dans tous les cas.

**Classification Métier / UI : Code métier**

**Composante : wd300hf.dll**

## Voir Aussi

- [Procédures et requêtes stockées (HFSQL Client/Serveur)](3044360.md)
- [Fonctions de gestion de HFSQL Client/Serveur](3044316.md)
- [<Variable Connexion>.MiseAJourRequête (Fonction)](1000022705.md)
