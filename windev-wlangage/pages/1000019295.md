---
title: "CertificatSélecteur (Fonction)"
english: "CertificateSelect"
type: "Fonction"
id: "1000019295"
module: "WDLang1"
category: "fonctions_certificat"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# CertificatSélecteur (Fonction)

> **Disponible :** **WINDEV** (Windows, Etats et Requêtes, Code Utilisateur (MCU))

Ouvre une fenêtre de sélection de certificats. Les certificats listés sont les certificats installés dans le magasin personnel.
Nouveauté Abonnement
**Remarque** : Les certificats présents sur une carte à puce sont également listés.
**Attention** : Cette fonctionnalité est disponible dans la version Abonnement, uniquement à partir de WINDEV Suite 2025 Update 1.
Pour plus de détails, consultez [Utiliser les nouveautés de WINDEV Suite 2025 spécifiques à l'abonnement](1410087359.md).

## Exemple

```wlangage
MonCertificat est un Certificat

// Ouverture du sélecteur de certificat de Windows
MonCertificat = CertificatSélecteur()

// Annulation ou erreur
SI MonCertificat.Nom = "" ALORS
	RETOUR
FIN
// Teste si le certificat sélectionné est valide pour signer
SI MonCertificat.ValidePourSignature = Faux ALORS
	Info("Le certificat sélectionné ne permet de pas de générer une signature.")
	RETOUR
FIN

// Récupération du buffer contenant la signature
bufSignature est un Buffer
bufSignature = CertificatSigneChaîne("Chaîne de caractères à signer", MonCertificat)

// Récupération du certificat contenu dans le buffer de la signature
MonCertificatExtrait est un Certificat
MonCertificatExtrait = CertificatExtrait(bufSignature)

// Gestion des erreurs
SI MonCertificatExtrait = Null ALORS
	RETOUR
FIN

// Ouverture de la fenêtre des propriétés du certificat
CertificatOuvrePropriétés(MonCertificatExtrait)
```

## Syntaxe

```wlangage
<Certificat> = CertificatSélecteur()
```

`<Certificat>` *Variable de type Certificat*
> Nom de la variable de type [Certificat](1000019287.md) à initialiser.
> Si la fenêtre de sélection a été validée, la variable de type [Certificat](1000019287.md) est initialisée avec les valeurs correspondant au certificat choisi.
>
> Si la fenêtre de sélection a été annulée, la variable de type [Certificat](1000019287.md) contient un certificat vide (par exemple son nom n'est pas rempli).

**Classification Métier / UI : Code UI**

**Composante : wd300std.dll**

## Voir Aussi

- [Gestion des certificats](1000019338.md)
- [Fonctions de gestion des certificats](1000019302.md)
- [Certificat (Type de variable)](1000019287.md)
