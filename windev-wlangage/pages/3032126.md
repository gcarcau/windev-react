---
title: "LDAPMode (Fonction)"
english: "LDAPMode"
type: "Fonction"
id: "3032126"
module: "WDLang3"
category: "fonctions_ldap"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# LDAPMode (Fonction)

> **Disponible :** **WINDEV** (Windows, Etats et Requêtes, Code Utilisateur (MCU)) · **WEBDEV** (Windows) · **Autres** (Procédures stockées)

Modifie le mode de fonctionnement d'une session LDAP.

## Exemple

```wlangage
// Procédure de connexion
...

// Désactivation du système de cache
LDAPMode("MaSessionLDAP", LDAP_CacheInactif)

// Récupération du nombre de racines
NbRacine est un entier = LDAPNbValeur("MaSessionLDAP", "namingContexts")

// Affichage des différentes racines du serveur
POUR Indice = 1 À NbRacine
	Info(LDAPValeur("MaSessionLDAP", "namingContexts", Indice)
FIN

// Réactivation du système de cache
LDAPMode("MaSessionLDAP", LDAP_CacheActif)

// Procédure de déconnexion
...
```

## Syntaxe

```wlangage
<Résultat> = LDAPMode(<Session LDAP> , <Option>)
```

`<Résultat>` *Booléen*
> - Vrai si la modification est effective,
> - Faux dans le cas contraire. La fonction [ErreurInfo](3013008.md) permet d'obtenir plus de détails sur l'erreur.
`<Session LDAP>` *Chaîne de caractères*
> Nom de la session LDAP manipulée.
`<Option>` *Constante de type entier*
> Indique le mode de fonctionnement de la session LDAP :
>
> | *LDAP_CacheActif* | Les différents appels aux fonctions LDAP utilisent le cache interne pour récupérer les différentes valeurs des attributs (mode par défaut). |
> | --- | --- |
> | *LDAP_CacheInactif* | Chaque appel aux fonctions [LDAPNbValeur](3032106.md) et [LDAPValeur](3032107.md) implique une communication avec le serveur. |

## Remarques

**Attention** : La constante *LDAP_CacheInactif* est à utiliser dans des cas très particuliers.
Pour lire des attributs opérationnels sur un serveur OpenLDAP, il faut utiliser cette option. En effet, par défaut, un serveur OpenLDAP ne renvoie pas les attributs opérationnels d'un objet, et la fonction [LDAPListeAttribut](3032104.md) ne liste pas non plus ces attributs.

**Composante : wd300com.dll**

## Voir Aussi

- [Fonctions LDAP](3032118.md)
