---
title: "RTFVersHTML (Fonction)"
english: "RTFToHTML"
type: "Fonction"
id: "3025049"
module: "WDLang1"
category: "fonctions_champ"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# RTFVersHTML (Fonction)

> **Disponible :** **WINDEV** (Windows, Linux, Etats et Requêtes, Code Utilisateur (MCU)) · **WEBDEV** (Windows, Linux) · **WINDEV Mobile** (iPhone/iPad, Widget IOS)

Renvoie la chaîne HTML correspondant à la chaîne RTF spécifiée.

## Exemple

> _(Linux, iPhone/iPad)_ // MaChaîne_RTF est une chaîne RTF// sHTML est une chaîne HTML// HTM_HTML1 est un champ HTMLsHTML = RTFVersHTML(MaChaîne_RTF)sTmp est une chaîne = "<HTML>" + sHTML + "</HTML>"HTM_HTML1 = sTmp

> _(WINDEV)_ // SAI_RTF est un champ RTF // sHTML est une chaîne HTML// HTM_HTML1 est un champ HTML// Utilisation de la gestion des tableaux et des images (version 23 et supérieures)sHTML = RTFVersHTML(SAI_RTF, rvhOptionAucune)sTmp est une chaîne = "<HTML>" + sHTML + "</HTML>"HTM_HTML1 = sTmp

## Syntaxe

```wlangage
<Résultat> = RTFVersHTML(<Texte RTF ou Nom du champ RTF> [, <Options>])
```

`<Résultat>` *Chaîne de caractères*
> Texte au format HTML
`<Texte RTF ou Nom du champ RTF>` *Chaîne de caractères*
> - Nom du champ de type RTF,
>    Ce type de paramètre n'est pas supporté.
> - Chaîne de caractères au format RTF.
`<Options>` *Constante de type Entier*
> Options de conversion :
>
> | *rvhOptionAucune* (Valeur par défaut) | Nouveau mode de conversion permettant de gérer les images et les tableaux.  Remarques :    - Le nouveau mode de conversion est disponible uniquement sous Windows. - Les tableaux imbriqués ne sont pas gérés. |
> | --- | --- |
> | *rvhOptionCompatible* | Permet de retrouver le fonctionnement de la version 22 et inférieure. Dans ce cas :   - Les tableaux ne sont pas gérés. - Si les images ont été intégrées au RTF (et non liées au document RTF), elles apparaîtront sous forme d'une suite de chiffres dans le document HTML. |
>
>
>
> > _(Linux, iPhone/iPad)_ Ce paramètre n'est pas disponible. Sur ces plateformes : Les tableaux ne sont pas gérés. Si les images ont été intégrées au RTF (et non liées au document RTF), elles apparaîtront sous forme d'une suite de chiffres dans le document HTML.

## Remarques

### Encodage des caractères
Par défaut, les caractères spéciaux et accentués ne sont pas encodés dans la chaîne HTML renvoyée par la fonction **RTFVersHTML**. Selon l'usage de ce HTML, il peut être nécessaire de l'encoder. Par exemple, si le HTML renvoyé est affecté au champ Editeur HTML, il sera nécessaire de le convertir en UTF8 avec la fonction [ChaîneVersUTF8](3024042.md).

## Liste des exemples associés :

| [image: Passage du format RTF au format HTML] | Exemples unitaires (WINDEV) : Passage du format RTF au format HTML     [ + ] Utilisation des fonctions RTFVersHTML et RTFVersTexte. |
| --- | --- |

**Composante : wd300rtf.dll**

## Voir Aussi

- [Fonctions de gestion des champs RTF](3025034.md)
- [Fonctions de gestion des champs](3025007.md)
