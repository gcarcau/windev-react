---
title: "<redisConnexion>.PipelineAjoute (Fonction)"
english: "<redisConnection>.PipelineAdd"
type: "Fonction"
id: "1410088283"
module: "WDLang4"
category: "fonctions_redis"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# <redisConnexion>.PipelineAjoute (Fonction)

> **Disponible :** **WINDEV** (Windows, Linux, Etats et Requêtes, Code Utilisateur (MCU)) · **WEBDEV** (Windows, Linux)

Ajoute une commande Redis dans un pipeline. Il est ainsi possible d'envoyer un groupe de commandes à un serveur Redis et d'optimiser les performances d'accès au réseau.
Nouveauté 2025
Cette fonction peut également être utilisée pour manipuler des données sur un serveur Valkey.

## Exemple

```wlangage
gRedis.PipelineAjoute("set p1 pv1")
gRedis.PipelineAjoute("set p2 pv2")
gRedis.PipelineAjoute("get p2")
gRedis.PipelineAjoute("get p1")

r1 est un redisRéponse = gRedis.PipelineRécupère()
r2 est un redisRéponse = gRedis.PipelineRécupère()
r3 est un redisRéponse = gRedis.PipelineRécupère()
r4 est un redisRéponse = gRedis.PipelineRécupère()
```

## Syntaxe

```wlangage
<Résultat> = <Connexion>.PipelineAjoute(<Commande> [, <Paramètre 1> [, <Paramètre N>]])
```

`<Résultat>` *Booléen*
> - Vrai si l'ajout de la commande Redis a été effectué,
> - Faux dans le cas contraire.
`<Connexion>` *Variable de type redisConnexion*
> Nom de la variable de type [redisConnexion](1000023527.md) correspondant à la connexion au serveur Redis.
`<Commande>` *Chaîne de caractères*
> Texte de la commande Redis à exécuter.
> Cette commande peut contenir des paramètres séparés par des espaces.
`<Paramètre 1>` *Type du paramètre optionnel*
> Premier paramètre optionnel de la commande. Ce paramètre permet de passer des chaînes contenant des espaces ou des valeurs binaires à la commande exécutée.
> Remarque : Les chaînes sont converties en UTF8 lors de l'envoi de la commande vers le serveur Redis.
> $~ ... |
`<Paramètre N>` *Type du paramètre optionnel*
> Nième paramètre de la commande. Ce paramètre permet de passer des chaînes contenant des espaces ou des valeurs binaires à la commande exécutée.
> Remarque : Les chaînes sont converties en UTF8 lors de l'envoi de la commande vers le serveur Redis.

## Remarques

### Fonctionnement du pipeline
Lorsque l'ensemble des commandes souhaitées auront été ajoutées dans le pipeline, les résultats pourront être récupérés avec la fonction [<redisConnexion>.PipelineRécupère](1410088285.md).
Pour ne pas récupérer les résultats, il est possible d'utiliser la fonction [<redisConnexion>.PipelineRAZ](1410088284.md).

Si la fonction [<redisConnexion>.PipelineRAZ](1410088284.md) n'a pas été utilisée, il doit y avoir autant d'appels à la fonction [<redisConnexion>.PipelineRécupère](1410088285.md) que d'appels à la fonction **<redisConnexion>.PipelineAjoute**.

**Classification Métier / UI : Code métier**

**Composante : wd300big.dll**

## Voir Aussi

- [Fonctions de gestion d'une base Big Data Redis (syntaxe préfixée)](1410088287.md)
- [<redisConnexion>.Connecte (Fonction)](1410088280.md)
- [<redisConnexion>.PipelineRAZ (Fonction)](1410088284.md)
- [<redisConnexion>.PipelineRécupère (Fonction)](1410088285.md)
- [redisConnexion (Type de variable)](1000023527.md)
- [RedisPipelineAjoute (Fonction)](1000023542.md)
