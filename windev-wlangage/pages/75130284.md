---
title: "Liste,Cherche (Langage externe)"
english: "List,Search"
type: "Langage externe"
id: "75130284"
module: "ExternalLanguages"
category: "fonctions_objet"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# Liste,Cherche (Langage externe)

> **Disponible :** **WINDEV** (Windows)

Recherche une valeur dans une liste. La recherche effectuée est une recherche à l'identique.

## Exemple

```wlangage
// En CAPPELWD("Liste,Cherche,ListeProd,000002");if WDEntier==-1 APPELWD("ERREUR,Element non trouvé");
```

```wlangage
// En PascalAPPELWD('Liste,Cherche,ListeProd,000002');if WDEntier=-1 then APPELWD('ERREUR,Element non trouvé');
```

```wlangage
// En VBcall APPELWD("Liste,Cherche,ListeProd,000002")if WDEntier=-1 then APPELWD("ERREUR,Element non trouvé")end if
```

## Syntaxe

```wlangage
Liste,Cherche(<Nom de la liste> , <Chaine recherchée>)
```

`<Nom de la liste>` *Chaîne de caractères*
> Nom du champ Liste à modifier.
`<Chaine recherchée>` *Chaîne de caractères*
> Valeur recherchée dans la liste. Cette valeur est recherchée dans toute la liste.

## Remarques

### Liste,Cherche

- l'indice de l'élément de la liste correspondant à la recherche,
- -1 si la chaîne recherchée n'est pas trouvée..

## Voir Aussi

- [Fonctions de gestion des objets (Langage externe)](75130327.md)
