---
title: "EspaceSignificatif (Fonction)"
english: "WithSpace"
type: "Fonction"
id: "3044142"
module: "WDLang4"
category: "fonctions_hyper_file"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# EspaceSignificatif (Fonction)

> **Disponible :** **WINDEV** (Windows, Linux, Etats et Requêtes, Code Utilisateur (MCU)) · **WEBDEV** (Windows, Linux) · **WINDEV Mobile** (iPhone/iPad, Widget IOS, Mac Catalyst) · **Autres** (Procédures stockées)

Ajoute ou supprime les espaces situés à droite d'une rubrique texte lors de sa lecture.
**Attention** : Cette fonction est prise en compte uniquement pour les fichiers de données dont l'option "Mode compatible 5.5 : rubriques texte complétées par des espaces" a été cochée sous l'éditeur d'analyses (onglet "Détail" de la description des fichiers de données).

## Exemple

```wlangage
// Gère les espaces significatifs (ajoute des espaces) pour tous les fichiers
EspaceSignificatif(Vrai)
```

## Syntaxe

```wlangage
<Résultat> = EspaceSignificatif([<Nom du fichier>] [, <Nom de la rubrique>] , <Options>)
```

`<Résultat>` *Booléen*
> Ancienne gestion des espaces significatifs.
`<Nom du fichier>` *Chaîne de caractères optionnelle*
> Nom du fichier de données manipulé. Si ce paramètre n'est pas spécifié, la fonction **EspaceSignificatif** va manipuler **tous les fichiers de données**.
>
> > _(Hyper File 5.5)_ Ce paramètre n'est pas pris en compte.
`<Nom de la rubrique>` *Chaîne de caractères optionnelle*
> Nom de la rubrique utilisée. Si ce paramètre n'est pas spécifié, la fonction **EspaceSignificatif** va manipuler **toutes les rubriques** du fichier de données spécifié.
>
> > _(Hyper File 5.5)_ Ce paramètre n'est pas pris en compte.
`<Options>` *Booléen*
> - Vrai : le contenu des rubriques texte est complété avec des espaces à droite.
> - Faux : les espaces à droite des rubriques texte sont supprimés.
>
>
> > _(Hyper File 5.5)_ La gestion des espaces significatifs sera effectuée pour tous les fichiers de données et pour toutes les rubriques texte de ces fichiers de données.

## Remarques

### Genéralisation de la gestion des espaces

- Pour généraliser le mode de gestion des espaces à **toutes les rubriques d'un fichier de données**, le paramètre <Nom de la rubrique> doit être égal à "*".
  Exemple :
  ```wlangage
  EspaceSignificatif(<Nom du fichier>, "*", <Options>)
  ```
- Pour généraliser le mode de gestion des espaces à **toutes les rubriques de tous les fichiers de données**, les paramètres <Nom du fichier> et <Nom de la rubrique> doivent être égaux à "*".
  Exemple :
  ```wlangage
  EspaceSignificatif("*", "*", <Options>)
  ```
  Dans ce cas, la fonction **EspaceSignificatif** renvoie Faux si la gestion des espaces était différente sur l'une au moins des rubriques concernées.
- Si seul le paramètre <Options> est précisé, le mode de gestion spécifié s'applique à tous les fichiers de données et à toutes les rubriques.

### Réplication et fonction EspaceSignificatif
La réplication est incompatible avec l'utilisation de la fonction **EspaceSignificatif** dans le code de l'application cliente. Cette fonction change le comportement de HFSQL. L'utilisation de cette fonction dans l'application cliente peut affecter l'efficacité de la réplication.
Depuis la version 21 update 3 (version 210065), les fonctions de synchronisation (réplication universelle programmée ou assistée) renvoient une erreur à l'application cliente si la fonction **EspaceSignificatif** a été appliquée pour l'un au moins des fichiers répliqués.

**Composante : wd300hf.dll**

## Voir Aussi

- [HLitRecherchePremier (Fonction)](3044036.md)
- [HLitRechercheDernier (Fonction)](3044034.md)
- [Fonctions de gestion HFSQL](3044156.md)
