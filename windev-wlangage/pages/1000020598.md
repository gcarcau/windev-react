---
title: "SélecteurAjoute (Fonction)"
english: "RadioButtonAdd"
type: "Fonction"
id: "1000020598"
module: "WDLang1"
category: "fonctions_selecteur"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# SélecteurAjoute (Fonction)

> **Disponible :** **WINDEV** (Windows, Linux, Etats et Requêtes, Code Utilisateur (MCU)) · **WINDEV Mobile** (Android, iPhone/iPad)

Ajoute une nouvelle option dans un champ Sélecteur. L'option est ajoutée à la suite des options existantes.

## Exemple

```wlangage
// Ajout de 2 options
SélecteurAjoute(SEL_Options, "Lire un livre sur une tablette")
SélecteurAjoute(SEL_Options, "Lire un livre de poche")
```

## Syntaxe

```wlangage
<Résultat> = SélecteurAjoute(<Champ Sélecteur> , <Libellé> [, <Sous-libellé> [, <Image>]])
```

`<Résultat>` *Entier*
> - Indice de l‘option ajoutée.
> - -1 en cas d'erreur. Pour plus de détails sur l'erreur, utilisez la fonction [ErreurInfo](3013008.md).
`<Champ Sélecteur>` *Nom de champ*
> Nom du champ Sélecteur dans lequel l'option doit être ajoutée. L'option est ajoutée à la suite des options existantes.
`<Libellé>` *Chaîne de caractères*
> Libellé de l'option à ajouter.
`<Sous-libellé>` *Chaîne de caractères optionnelle*
> Sous-libellé de l'option à ajouter.
> Si ce paramètre n'est pas spécifié, l'option ajoutée n'a pas de sous-libellé.
> Si ce paramètre est spécifié, le sous-libellé sera affiché même si le sélecteur utilise un mode d'affichage sans sous-libellé.
>
> > _(Android)_ Ce paramètre n'est pas disponible.
`<Image>` *Chaîne de caractères optionnelle*
> Image associée à l'option à ajouter.
> Si ce paramètre n'est pas spécifié, l'option ajoutée n'a pas d'image.
> Si ce paramètre est spécifié, l'image sera affichée uniquement si le sélecteur utilise un mode d'affichage "Style Vista" (avec image).
>
> > _(Android)_ Ce paramètre n'est pas disponible.

## Remarques

- Les caractéristiques de l'option ajoutée peuvent également être modifiées grâce aux propriétés suivantes utilisées sur l'option :
  - [Libellé](2510053.md) pour modifier le libellé.
  - [SousLibellé](1000017232.md) pour modifier le sous-libellé.
  - [Image](2510034.md) pour modifier l'image.
- Lors de l'ajout d'options dans un champ Sélecteur, la taille du champ n'est pas modifiée. Si le champ n'est pas suffisamment grand, les options ajoutées pourront ne pas être visibles. Il est possible d'augmenter la taille du champ avec les propriétés [Largeur](2510059.md) et [Hauteur](2510050.md).
- Pour ajouter une option à une position spécifique, utilisez la fonction [SélecteurInsère](1000020599.md).
- **Utilisation des valeurs renvoyées** : Si le sélecteur utilise les valeurs renvoyées, il est possible de définir la valeur renvoyée par l'option ajoutée grâce à la propriété [ValeurRenvoyée](2510134.md).
  Par exemple :
  ```wlangage
  nIndice est un entier
  nIndice = SélecteurAjoute(SEL_Option, "Lire un livre sur une tablette")
  SEL_Option[nIndice].ValeurRenvoyée = "Tablette"
  // Sélection de la nouvelle option
  SEL_Option = "Tablette"
  ```

**Composante : wd300obj.dll**

## Voir Aussi

- [SélecteurInsère (Fonction)](1000020599.md)
- [SélecteurSupprime (Fonction)](1000020600.md)
- [Le champ Sélecteur](1013228.md)
- [Fonctions de gestion des champs Sélecteur](1000020616.md)
- [<Sélecteur>.Ajoute (Fonction)](1000021186.md)
