---
title: "HTMLActiveDébogueur (Fonction)"
english: "HTMLEnableDebugger"
type: "Fonction"
id: "1410087849"
module: "WDLang1"
category: "fonctions_affichage_html"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# HTMLActiveDébogueur (Fonction)

> **Disponible :** **WINDEV** (Windows, Code Utilisateur (MCU))

Active le débogueur HTML/Javascript sur les champs HTML basé sur CEF (Chromium) : champ d'affichage HTML ou champ Editeur HTML.
Pour déboguer les champs :

- Lancez le navigateur Chrome.
- Saisissez l'adresse suivante :
  ```wlangage
  chrome://inspect/#devices
  ```
- Dans la page qui s'affiche, cochez "Discover network targets" si nécessaire.
- Cliquez sur le bouton "Configure".
- Dans la fenêtre popup qui s'affiche, saisissez l'adresse suivante :
  ```wlangage
  http://localhost:8088/
  ```
   où 8088 correspond au port que vous avez choisi.
- Cliquez sur "Done".

**Remarque** : Jusqu'à la version 2025 Update 3, la manipulation à effectuer était la suivante :

- Lancer le navigateur Chrome puis taper l'adresse suivante :
  ```wlangage
  http://localhost:8088/
  ```
- Cliquer sur un des liens apparaissant dans chrome (1 lien par champ).

## Exemple

```wlangage
// Code d'initialisation du projet
SI ChargeParamètre("Debug") = "1" ALORS
      HTMLActiveDébogueur(8081)
FIN
```

## Syntaxe

```wlangage
HTMLActiveDébogueur(<Numéro de port>)
```

`<Numéro de port>` *Entier*
> Numéro de port utilisé pour le débogage. Par défaut, ce numéro correspond à 8088.

## Remarques

- Cette fonction doit être appelée avant la création de tout champ HTML (champ d'affichage HTML ou champ Editeur HTML), lors de l'initialisation du projet par exemple.
- Le débogueur HTML/Javascript (Devtools) peut être affiché avec l'URL suivante :
  ```wlangage
  http://nom_machine:num_port/
  ```
  Par exemple :
  ```wlangage
  http://localhost:8088/
  ```
- Le débogueur est activé par défaut en GO mais il n'est pas activé durant l'exécution de l'application. Il permet par exemple de tracer le code Javascript ajouté avec la fonction [ExécuteJS](1000023014.md).

**Classification Métier / UI : Code neutre**

**Composante : wd300obj.dll**

## Voir Aussi

- [Fonctions de gestion des champs d'affichage HTML](1000026264.md)
- [Fonctions de gestion du champ Editeur HTML](1000026295.md)
