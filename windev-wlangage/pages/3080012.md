---
title: "xlsTitreColonne (Fonction)"
english: "xlsColumnTitle"
type: "Fonction"
id: "3080012"
module: "WDLang5"
category: "fonctions_xls"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# xlsTitreColonne (Fonction)

> **Disponible :** **WINDEV** (Windows, Linux, Etats et Requêtes, Code Utilisateur (MCU)) · **WEBDEV** (Windows, Linux) · **WINDEV Mobile** (Android, Widget Android, iPhone/iPad)

Récupère l'intitulé d'une colonne d'un fichier XLS. Selon le mode d'ouverture du fichier XLS, le résultat peut varier :

- En mode multi-feuilles, le titre de la colonne est recherché pour la feuille sélectionnée avec la fonction [xlsFeuilleEnCours](3080022.md).
- En mode compatible, le titre de la colonne est toujours recherché pour la première feuille du classeur.

**Remarque** : L'intitulé d'une colonne d'un fichier XLS ne peut être récupéré que si :

- Le fichier XLS existe.
- Le fichier XLS est ouvert.

Nouveauté 2025

> _(Android)_ Cette fonction est désormais disponible pour les applications Android.

> _(Widget Android)_ Cette fonction est désormais disponible en mode Widget Android.

> _(iPhone/iPad)_ Cette fonction est désormais disponible pour les applications iPhone/iPad.

## Exemple

```wlangage
// Ouverture d'un fichier XLS
IDFichierXLS = xlsOuvre("C:\MonRépertoire\Fichier.XLS")
SI IDFichierXLS <> -1 ALORS
	// Récupération de l'intitulé d'une colonne
	ResTitreColonne = xlsTitreColonne(IDFichierXLS, 4)
	...
FIN
```

## Syntaxe

#### Syntaxe utilisant le type xlsDocument

```wlangage
<Résultat> = xlsTitreColonne(<Document XLS> , <Numéro de la colonne> [, <Colonnes vides>])
```

`<Résultat>` *Chaîne de caractères*
> - Intitulé de la colonne spécifiée,
> - Constante *xlsTitreErreur* en cas d'erreur. Pour connaître le libellé de l'erreur, utilisez la fonction [xlsMsgErreur](3080016.md) ou la fonction [ErreurInfo](3013008.md) avec la constante *errMessage*.
`<Document XLS>` *Variable de type xlsDocument*
> Nom de la variable de type [xlsDocument](1000017464.md) correspondant au fichier XLS à manipuler.
`<Numéro de la colonne>` *Entier*
> Numéro de la colonne à lire.
`<Colonnes vides>` *Booléen optionnel*
> Mode de prise en compte des colonnes vides :
>
> | *Faux* | Les colonnes entièrement vides sont prises en compte |
> | --- | --- |
> | *Vrai* (par défaut) | Les colonnes entièrement vides ne sont pas prises en compte |

#### Syntaxe conservée par compatibilité avec la version 12

```wlangage
<Résultat> = xlsTitreColonne(<Identifiant du fichier XLS> , <Numéro de la colonne> [, <Colonnes vides>])
```

`<Résultat>` *Chaîne de caractères*
> - Intitulé de la colonne spécifiée,
> - Constante *xlsTitreErreur* en cas d'erreur. Pour connaître le libellé de l'erreur, utilisez la fonction [xlsMsgErreur](3080016.md) ou la fonction [ErreurInfo](3013008.md) avec la constante *errMessage*.
`<Identifiant du fichier XLS>` *Entier*
> Identifiant du fichier XLS à manipuler, renvoyé par la fonction [xlsOuvre](3080017.md).
`<Numéro de la colonne>` *Entier*
> Numéro de la colonne à lire.
`<Colonnes vides>` *Booléen optionnel*
> Mode de prise en compte des colonnes vides :
>
> | *Faux* | Les colonnes entièrement vides sont prises en compte |
> | --- | --- |
> | *Vrai* (par défaut) | Les colonnes entièrement vides ne sont pas prises en compte |

## Remarques

### Intitulé de colonne
Les colonnes vides peuvent être prises en compte ou non. Par exemple :

```wlangage
xlsTitreColonne(IDFichierXLS, 3, Vrai) // Retourne "Age"
xlsTitreColonne(IDFichierXLS, 3, Faux) // Retourne ""
```

### Gestion des erreurs
La fonction **xlsTitreColonne** génère une erreur dans les cas suivants :

- La colonne spécifiée n'existe pas (colonne négative par exemple).
- Le fichier XLS spécifié n'est pas ouvert.

**Composante : wd300xls.dll**

## Voir Aussi

- [xlsTypeColonne (Fonction)](3080019.md)
- [Manipulez vos fichiers XLS](3080020.md)
- [Fonctions de gestion des fichiers XLS](3080009.md)
