---
title: "Manipuler un champ Editeur d'images pour mobile par programmation"
id: "1410087215"
module: "WDControls"
category: "champ_editeur_image_wm"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# Manipuler un champ Editeur d'images pour mobile par programmation

> **Disponible :** **WINDEV Mobile** (Android, iPhone/iPad)

## Présentation

WINDEV Mobile permet de manipuler directement un champ Editeur d'images mobile par programmation.
La variable du champ Editeur d'images mobile permet de manipuler l'image affichée dans le champ.

## Afficher une image dans un champ Editeur d'images

Pour afficher une image dans un champ Editeur d'images, une simple affectation suffit. Il est possible d'utiliser le nom d'un fichier ou une variable de type [Image](1000019650.md).
Quelques exemples :

- affectation directe :
  ```wlangage
  EI_MonImage = "C:\Temp\MonImage.bmp"
  ```
- affectation par variable :
  ```wlangage
  CheminImage = fRepEnCours() + "\MonImage.bmp"
  EI_MonImage = CheminImage
  ```
- affectation par variable de type [Image](1000019650.md) :
  ```wlangage
  MonImage est une Image
  MonImage.Largeur = 500
  MonImage.Hauteur = 100
  // Chargement à partir d'un fichier
  MonImage = dChargeImage("image.png")
  // Dessin dans l'image
  dDébutDessin(MonImage)
  // Ecriture d'un texte
  dPolice("Arial", 12, iNormal, 0)
  dTexte(4, 0, "J'écris mon texte dans un champ Image !")
  // Fin du dessin
  dFinDessin(MonImage)
  // Affectation à un champ Editeur d'images mobile
  EI_MonImage = MonImage
  ```

Remarque : Un champ Editeur d'images mobile peut aussi être initialisé à partir d'une variable de type Buffer ou de type texte contenant la valeur de l'image en binaire.

## Récupérer l'image affichée dans le champ Editeur d'images

L'image affichée et manipulée dans le champ Editeur d'images peut être récupérée dans une variable de type [Image](1000019650.md).
Par exemple :

```wlangage
MonImageModifiée est une Image
MonImageModifiée = EI_MonImage
```

Remarque : L'image récupérée contient uniquement les modifications validées par l'utilisateur. Si des modifications sont en cours dans le champ et n'ont pas été validées, elles ne seront pas prises en compte dans la variable [Image](1000019650.md) renvoyée.

L'image peut ensuite être sauvegardée (fonction [dSauveImagePNG](3029027.md), fonction [AlbumSauve](1000020191.md) par exemple).

## Propriétés WLangage associées au champ Editeur d'images mobile

Pour connaître la liste complète des propriétés du WLangage pouvant être utilisées avec un champ de type Editeur d'images mobile, consultez [Propriétés associées au champ Editeur d'images mobile](1410087213.md).

## Voir Aussi

- [Le champ Editeur d'images pour mobile](1410087209.md)
