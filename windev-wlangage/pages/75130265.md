---
title: "Ecran,Saisie (Langage externe)"
english: "Screen,Input"
type: "Langage externe"
id: "75130265"
module: "ExternalLanguages"
category: "fonctions_objet"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# Ecran,Saisie (Langage externe)

> **Disponible :** **WINDEV** (Windows)

Lance la saisie des champs d'une fenêtre.

## Exemple

```wlangage
// En Cwhile (strcmp(Touche,"ESC")!=0) { APPELWD("Ecran,Saisie"); strcpy(Touche,WDTouche);
```

```wlangage
// En PascalWhile Touche<>'ESC' DOBegin APPELWD('Ecran,Saisie'); Touche:=WdTouche; ...End;
```

```wlangage
// En VBWhile Touche<>"ESC" call APPELWD("Ecran,Saisie") Touche=WdTouche ...WEnd
```

## Syntaxe

```wlangage
Ecran,Saisie(</CR> , </NONBLOQUANT>)
```

`</CR>` **
> Permet de détecter si une erreur est intervenue. Si ce paramètre est précisé, en cas d'erreur WdTouche contient "*E*".
`</NONBLOQUANT>` **
> Permet de quitter immédiatement la saisie sans attendre forcément de résultat. Si ce paramètre est précisé : "*N*" est retourné dans WdTouche si aucune action n'a été effectuée.

## Remarques

- En sortie de saisie, la variable WdTouche contient le compte-rendu de la saisie modifié en WLangage par RenvoieWdTouche ou WdTouche=<Code Touche>.
- En sortie de saisie d'un menu, la variable WdTouche contient "*M*" et la suite des lettres d'appel des options sélectionnées est dans WdChaine.

## Voir Aussi

- [Fonctions de gestion des objets (Langage externe)](75130327.md)
