---
title: "ServiceDémarre (Fonction)"
english: "ServiceStart"
type: "Fonction"
id: "1000017110"
module: "WDLang1"
category: "fonctions_service"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# ServiceDémarre (Fonction)

> **Disponible :** **WINDEV** (Windows, Linux, Etats et Requêtes, Code Utilisateur (MCU)) · **WEBDEV** (Windows, Linux)

Démarre un service. Ce service peut être un service présent sur le poste local ou sur un poste distant.

## Exemple

```wlangage
// Démarre le service présent dans la table TABLE_Service
SI ServiceDémarre(COL_Nom[TABLE_Service]) = Faux ALORS
	Erreur(ErreurInfo(errMessage))
FIN
```

## Syntaxe

```wlangage
<Résultat> = ServiceDémarre(<Nom du service> [, <Ligne de commande> [, <Poste distant>]])
```

`<Résultat>` *Booléen*
> - Vrai si la demande de démarrage a été envoyée,
> - Faux dans le cas contraire. Pour plus de détails sur l'erreur, utilisez la fonction [ErreurInfo](3013008.md).
`<Nom du service>` *Chaîne de caractères*
> Nom du service à démarrer.
`<Ligne de commande>` *Chaîne de caractères optionnelle*
> Ligne de commande à passer au service avant de le démarrer. Ce paramètre ne doit pas être spécifié si vous utilisez un [service créé avec WINDEV](1000017114.md).
`<Poste distant>` *Chaîne de caractères optionnelle*
> Adresse IP du poste distant au format xxx.xxx.xxx.xxx (obtenu par la fonction [NetAdresseIP](3056023.md)) ou nom du poste distant (obtenu par la fonction [NetNomMachine](3056014.md)). Ce paramètre permet de démarrer un service sur un autre poste. Pour utiliser cette option, l'utilisateur doit être administrateur de la machine distante.
> Si ce paramètre n'est pas précisé ou correspond à une chaîne vide, le service est manipulé en local.

## Remarques

> _(WINDEV, WEBDEV - Code Serveur, Etats et Requêtes, Windows, Code Utilisateur (MCU), Ajax)_ Mode de fonctionnementLa fonction ServiceDémarre envoie au gestionnaire de services la demande de démarrage d'un service. Cette opération peut ne pas être effectuée immédiatement. Pour connaître l'état du service, utilisez la fonction ServiceAttendEtat.

**Composante : wd300std.dll**

## Voir Aussi

- [Fonctions de gestion des services](1000017117.md)
