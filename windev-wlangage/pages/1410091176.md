---
title: "<Variable sshSession>.Commande (Fonction)"
english: "<sshSession variable>.Command"
type: "Fonction"
id: "1410091176"
module: "WDLang3"
category: "fonctions_ssh"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# <Variable sshSession>.Commande (Fonction)

> **Disponible :** **WINDEV** (Windows, Linux, Etats et Requêtes, Code Utilisateur (MCU)) · **WEBDEV** (Windows, Linux) · **WINDEV Mobile** (Android, Widget Android, iPhone/iPad, Widget IOS, Mac Catalyst)

Envoie une commande à un serveur SSH. La session est ouverte et refermée automatiquement après l'envoi de la commande.

## Exemple

```wlangage
cMaSession est une sshSession
cMaSession.Adresse = "127.0.0.1"
cMaSession.Port = 22
cMaSession.Utilisateur = "login"
cMaSession.MotDePasseUtilisateur = "pass"
nExitCode est un entier
sSortie est une chaîne ANSI
sSortieErr est une chaîne ANSI
(nExitCode, sSortie, sSortieErr) = cMaSession.Commande(SAI_Commande)
SI ErreurDétectée ALORS
	Erreur(ErreurInfo(errComplet))
	RETOUR
FIN
SAI_ExitCode = nExitCode
SAI_StdOut = UTF8VersChaîne(sSortie)
SAI_StdErr = UTF8VersChaîne(sSortieErr)
```

```wlangage
cMaSession1 est une sshSession
cMaSession1.Adresse = "127.0.0.1"
cMaSession1.Utilisateur = "login"
cMaSession1.MotDePasseUtilisateur = "pass"

bufDonnéesEnvoyées est un Buffer = 'Il est [%HeureVersChaîne(HeureSys, "HH:mm:SS")%]'
nExitCode est un entier
sSortie est une chaîne ANSI
sSortieErr est une chaîne ANSI

// Exécution d'une script Linux qui stocke les données envoyées dans un fichier
// puis qui affiche le contenu du fichier sur la sortie standard
(nExitCode, sSortie, sSortieErr) = cMaSession1.Commande(...
	"echo $(</dev/stdin) > donnees_envoyees.txt; cat donnees_envoyees.txt", bufDonnéesEnvoyées)
Trace(sSortie) //Affiche le contenu du buffer bufEntréeStandard
```

## Syntaxe

```wlangage
(<Résultat>, <Données reçues>, <Données en cas d'erreur>) = <Session SSH>.Commande(<Commande> [, <Données à envoyer>])
```

`<Résultat>` *Entier*
> Code de sortie de la commande.
`<Données reçues>` *Buffer*
> Données reçues du serveur.
`<Données en cas d'erreur>` *Buffer*
> Données reçues du serveur en cas d'erreur.
`<Session SSH>` *Variable de type sshSession*
> Nom de la variable de type [sshSession](1000021157.md) correspondant à la session SSH sur laquelle la commande doit être exécutée.
`<Commande>` *Chaîne de caractères*
> Commande à envoyer au serveur SSH.
`<Données à envoyer>` *Buffer optionnel*
> Données à envoyer au serveur. Ces données seront lisibles depuis l'entrée standard (stdin).

## Remarques

Nouveauté 2025

### Keep Alive des sessions SSH
Il est possible d'activer la fonctionnalité "Keep Alive" lors de l'ouverture de la session SSH (propriété **IntervalleKeepAlive** de la variable de type [sshSession](1000021157.md)). Le "Keep Alive" consiste à envoyer dans un délai plus ou moins long deux requêtes au serveur afin de conserver la connexion ouverte et de vérifier si le serveur répond toujours. La propriété **IntervalleKeepAlive** permet de définir la durée entre ces deux requêtes.
Si le serveur SSH s'arrête brutalement lors d'une commande "longue" exécutée via la fonction **<Variable sshSession>.Commande**, l'activation du "Keep Alive" peut permettre de détecter la déconnexion de l'application au serveur.

**Classification Métier / UI : Code métier**

**Composante : wd300com.dll**

## Voir Aussi

- [Dialoguer avec un serveur SSH (syntaxe préfixée)](1000021169.md)
- [Fonctions SSH (syntaxe préfixée)](1000021168.md)
- [sshSession (Type de variable)](1000021157.md)
- [<Variable sshSession>.ConnecteCommande (Fonction)](1410091177.md)
- [<Variable sshSession>.DéconnecteCommande (Fonction)](1410091178.md)
- [SSHCommande (Fonction)](1000021143.md)
