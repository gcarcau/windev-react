---
title: "Opérateur de conversion explicite sur les classes"
id: "6010019"
module: "OOP"
category: "poo"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# Opérateur de conversion explicite sur les classes

> **Disponible :** **WINDEV** (Windows, Linux, Java, Code Utilisateur (MCU)) · **WEBDEV** (Windows, Linux, WEBDEV - Code Navigateur) · **WINDEV Mobile** (Android, Widget Android, iPhone/iPad, Widget IOS, Apple Watch, Mac Catalyst)

L'opérateur de cast (downcast) permet d'effectuer une conversion d'une classe de base vers une classe dérivée sans utiliser de variable locale.

## Exemple

```wlangage
// appel d’une méthode de la classe dérivée
une_instance est ClasseBase <- Null
SI une_instance est ClasseDérivée ALORS
	((ClasseDérivée)une_instance).MéthodeDérivée()
FIN
```

## Syntaxe

#### Conversion statique : Provoque une exception en exécution si la conversion est illicite

```wlangage
((<Classe dérivée>) <Instance de la classe de base>)
```

`<Classe dérivée>` **
> Classe héritant de la classe de base.
`<Instance de la classe de base>` **
> Instance de la classe de base à convertir vers la <Classe dérivée>.

#### Conversion dynamique : Renvoie Null si la conversion est illicite

```wlangage
((<Classe dérivée> ?) <Instance de la classe de base>)
```

`<Classe dérivée>` **
> Classe héritant de la classe de base.
`<Instance de la classe de base>` **
> Instance de la classe de base à convertir vers la <Classe dérivée>.
**Détail** : La conversion statique ne provoque pas d'erreur en exécution si la conversion est illicite. Un objet fictif est renvoyé pour lequel la comparaison à **Null** renvoie Vrai. Une erreur de compilation sera affichée si cet objet fictif est manipulé.

## Voir Aussi

- [Héritage de classe](6010007.md)
