---
title: "HJSONVersEnregistrement (Fonction)"
english: "HJSONToRecord"
type: "Fonction"
id: "1000021910"
module: "WDLang4"
category: "fonctions_hyper_file"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# HJSONVersEnregistrement (Fonction)

> **Disponible :** **WINDEV** (Windows, Linux, Etats et Requêtes, Code Utilisateur (MCU)) · **WEBDEV** (Windows, Linux) · **WINDEV Mobile** (Android, Widget Android, iPhone/iPad, Widget IOS, Mac Catalyst) · **Autres** (Procédures stockées)

Importe des données JSON vers l'enregistrement courant.

## Exemple

```wlangage
soit sChaîne1 = "{ ""Nom"":""Tintin"", ""Prenom"":[""Milou"", ""IDEFIX""] }"
soit b1 = HJSONVersEnregistrement(Client, sChaîne1)
SI ErreurDétectée = Vrai ALORS
	// Message complet de l'erreur principale
	Trace(HErreurInfo())
	// Parcours des sous-erreurs (s'il y en a)
	POUR i = 1 _À_ ErreurInfo(errNombreSousErreur)
		// Message complet de chaque sous-erreur
		Trace("Sous-erreur " + i + " : " + ErreurInfo(errMessage, i))
	FIN
FIN
HAjoute(Client)
```

## Syntaxe

```wlangage
<Résultat> = HJSONVersEnregistrement(<Fichier de données> , <Données JSON> [, <Liste des rubriques destination> [, <Liste des "clés" JSON à importer>]])
```

`<Résultat>` *Booléen*
> - Vrai si aucun problème n'a été rencontré pendant l'importation,
> - Faux dans le cas contraire. La fonction [HErreurInfo](3044071.md) permet d'obtenir plus d'informations sur le problème rencontré.
`<Fichier de données>` *Chaîne de caractères optionnelle*
> Nom du fichier de données HFSQL manipulé. Les données JSON sont importées dans l'enregistrement courant.
`<Données JSON>` *Chaîne de caractères ou variable de type JSON*
> Données JSON à convertir vers l'enregistrement. Ces données peuvent correspondre à une chaîne de caractères au format JSON ou à une variable de type [JSON](1000023611.md).
`<Liste des rubriques destination>` *Chaîne de caractères optionnelle*
> Liste des noms des rubriques du fichier de données dans lequel l'importation des données JSON est effectuée. Les noms des rubriques peuvent être délimités par des simples quotes et sont séparés par des virgules ou RC (Retour-Chariot).
> Les noms des rubriques de type clé composée, mémo binaire et de type binaire sont ignorés.
> Les rubriques ne figurant pas dans cette liste seront initialisées avec leur valeur par défaut décrite dans l'analyse.
> Si ce paramètre n'est pas précisé ou correspond à une chaîne vide (""), toutes les rubriques sont importées (sauf les clés composées, les rubriques binaire et mémos binaire) dans l'ordre physique de description.
`<Liste des "clés" JSON à importer>` *Chaîne de caractères optionnelle*
> Liste des clés associées aux valeurs des données JSON à importer.
> Si ce paramètre n'est pas précisé ou correspond à une chaîne vide (""), les valeurs associées à toutes les clés JSON sont importées.
> Si une rubrique du fichier de données est manquante dans les données JSON, une erreur non fatale est renvoyée.

## Remarques

### Mode d'importation
Si la liste des clés JSON est précisée : l'importation recherchera des "valeurs JSON" dont le nom de la clé associée est identique au nom des rubriques du fichier de données.
Exemple : Le fichier de données décrit dans l'analyse contient les rubriques suivantes RUB1, RUB2, ... Les données JSON devront donc avoir une structure du type ci-dessous :

```wlangage
{"Fichier": {  "RUB1":Donnée de RUB1,  "RUB2":Donnée de RUB2,  ...  }}
```

Les noms des clés associées aux valeurs JSON qui n'existent pas dans l'analyse ou ne sont pas donnés dans la liste des rubriques seront ignorés lors de l'importation.

Si la liste des clés JSON n'est pas précisée, la valeur des clés JSON est mise dans la rubrique du fichier de données HFSQL qui porte le même nom que la clé JSON.

### Encodage
L'encodage utilisé correspond au standard JSON :

- utilisation d'un encodage en ASCII 7 bits, soit les 128 premiers caractères, les caractères non accentués,
- utilisation d'un encodage JSON des autres caractères : "\u" suivi du code du caractère en hexadécimal sur 4.

Exemple : pour "é" (code ASCII 233, code hexadécimal E9), l'encodage correspond à "\u00E9".

**Classification Métier / UI : Code métier**

**Composante : wd300hf.dll**

## Voir Aussi

- [<Source>.JSONVersEnregistrement (Fonction)](1000025009.md)
- [HEnregistrementVersJSON (Fonction)](1000021909.md)
- [Fonctions de gestion HFSQL](3044156.md)
- [HImporteJSON (Fonction)](1000021965.md)
- [Fonctions de manipulation des variables JSON](1410091296.md)
