---
title: "Projet (Langage externe)"
english: "Project"
type: "Langage externe"
id: "75130302"
module: "ExternalLanguages"
category: "fonctions_objet"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# Projet (Langage externe)

> **Disponible :** **WINDEV** (Windows)

Ouvre et exécute un projet WINDEV. La première fenêtre du projet est affichée et la saisie de cette fenêtre est automatiquement activée.

## Exemple

```wlangage
// En CAPPELWD("Projet,Compta");
```

```wlangage
// En PascalAPPELWD('Projet,Compta');
```

```wlangage
// En VBcall APPELWD("Projet,Compta")
```

## Syntaxe

```wlangage
Projet(<Nom du projet>)
```

`<Nom du projet>` *Chaîne de caractères*
> Nom du projet à lancer. Lors de l'exécution du projet, la feuille de styles de la bibliothèque du projet est chargée.

## Remarques

- Important: la fonction **Projet** doit être appelée après la fonction [Bibli,Disque](7513024.md) et avant la fonction HInit.
- Si une première fenêtre a été sélectionnée dans la liste des projets, cette fenêtre sera ouverte par la fonction **Projet**. Pour n'avoir aucune fenêtre ouverte, il faut sélectionner <Aucune> comme première fenêtre dans la liste des fenêtres du projet.
- Un programme en C, en Pascal peut par exemple contenir uniquement la fonction de lancement du projet.
- Si une erreur est survenue lors de l'ouverture, WdTouche contient "*E*".
- Si une fenêtre fait appel à des variables ou procédures globales, il faut exécuter le projet par la fonction Projet avant d'ouvrir la fenêtre, sinon les variables ou procédures globales ne seront pas reconnues, ni la feuille de styles.
- Plusieurs projets peuvent être appelés dans un même programme. Pour cela, il faut fermer le projet en cours avant d'en ouvrir un autre. Pour fermer le projet en cours, il faut utiliser la syntaxe suivante :
  - Exemple en C :
    ```wlangage
    HInit(&stTabCtx[0],NBCTX);APPELWD("BIBLI,DISQUE,MABIBLI");APPELWD("PROJET,MonProjet");
    ```
  - Exemple en Pascal :
    ```wlangage
    HInit;APPELWD('PROJET, BIBLI,DISQUE,MABIBLI ');APPELWD('PROJET, MonProjet ');
    ```
  - Exemple en VB :
    ```wlangage
    call HInitcall APPELWD("PROJET, BIBLI,DISQUE,MABIBLI ")call APPELWD("PROJET, MonProjet ")
    ```

## Voir Aussi

- [Fonctions de gestion des objets (Langage externe)](75130327.md)
