---
title: "Paramètres de la procédure gérant la réponse d'un serveur HTTP"
id: "1000025791"
module: "WDLang3"
category: "fonctions_http"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# Paramètres de la procédure gérant la réponse d'un serveur HTTP

> **Disponible :** **WINDEV** (Windows, Linux, Etats et Requêtes, Code Utilisateur (MCU)) · **WEBDEV** (Windows, Linux) · **WINDEV Mobile** (iPhone/iPad, Widget IOS)

Procédure WLangage (également nommée "callback") appelée par la fonction [HTTPEnvoie](1000021183.md) ou la fonction [HTTPEnvoieAsynchrone](1410091208.md) lors de la réponse du serveur HTTP.

## Exemple

```wlangage
cMaRequête est un httpRequête
cMaRequête.URL = "http://www.monsite.fr/api/streamdata"
cMaRequête.ProcédureEntête = procédure(cMonEntête est un httpRéponse) {
	SI cMonEntête.CodeEtat = 200 ALORS Trace("Requête envoyée.")
}
cMaRequête.ProcédureDonnées = procédure(bufMorceau est un Buffer) {
	Trace("Je traite [%Taille(bufMorceau)%] octets de données")
}
PROCÉDURE INTERNE procFin(cMaRéponse est un httpRéponse)
	Trace("requête http terminée")
FIN
HTTPEnvoieAsynchrone(cMaRequête, procFin)
```

## Syntaxe

```wlangage
EnvoieAsynchrone_Callback(<Réponse>)
```

`<Réponse>` *Variable de type httpRéponse*
> Variable de type [httpRéponse](1000021165.md) contenant la réponse reçue du serveur HTTP.

## Remarques

### Gestion des erreurs
Pour gérer les erreurs dans cette procédure :

- La variable [ErreurDétectée](3087001.md) et la fonction [ErreurInfo](3013008.md) permettent de savoir si le serveur contacté n'est pas joignable.
- La propriété **CodeEtat** de la variable de type [httpRéponse](1000021165.md) permet de connaître la réponse du serveur et de détecter une erreur éventuelle. Par défaut 200 indique que la requête est acceptée, une éventuelle erreur sera détaillée par un autre code HTTP.

### Mode Chunked : Récupération du résultat de la requête en morceaux
Si le serveur HTTP renvoie une requête en morceaux, il est possible de gérer la récupération des données en précisant dans la variable httpRequête :

- Une procédure de lecture d'en-tête (propriété **ProcédureEntête**).
- Une procédure de récupération des données (propriété **ProcédureDonnées**).

Dans ce cas, la procédure callback appelée par la fonction [HTTPEnvoie](1000021183.md) ou la fonction [HTTPEnvoieAsynchrone](1410091208.md) permet de récupérer la réponse du serveur via une variable de type [httpRéponse](1000021165.md).

Remarques :

- Si la propriété **ProcédureDonnées** était précisée dans la requête envoyée, la propriété **httpRéponse.Contenu** sera vide.
- L'entête reçue dans cette callback peut être plus grande que celle reçue au début. L'en-tête peut en effet contenir des informations spécifiques liées à l'envoi fragmenté des données (chunked trailer).

### Erreur de sécurité dans une transaction sécurisée
Lors d'une transaction sécurisée, certaines erreurs de sécurité peuvent faire échouer la requête :

- certificat invalide ou ne provenant pas d'une société connue.
- nom du site figurant dans le certificat ne correspond pas à un serveur.
- date du certificat non-valide ou expiré.
- renvoi vers un serveur non sécurisé.

Ces erreurs sont renvoyées par la fonction [ErreurInfo](3013008.md).

Si une de ces erreurs est rencontrée, il est possible d'exécuter à nouveau la requête en ignorant ces erreurs. Pour cela, il suffit de modifier la propriété **IgnoreErreur** de la variable de type [httpRequête](1000021158.md) :

| Erreur renvoyée par la fonction [ErreurInfo](3013008.md)  (avec la constante *errCode*) |  | Description |
| --- | --- | --- |
| *httpErreurCertificatInvalide* Certificat invalide ou ne provenant pas d'une société connue. | *httpIgnoreCertificatInvalide* | Le certificat est ignoré. |
| *httpErreurNomCertificatInvalide* Nom du site figurant dans le certificat ne correspond pas à un serveur. | *httpIgnoreNomCertificatInvalide* | Le nom du site figurant dans le certificat est ignoré. |
| *httpErreurCertificatExpiré* Date du certificat non-valide ou expiré. | *httpIgnoreCertificatExpiré* | La date du certificat est ignorée. |
|  | *httpIgnoreDéprécié* | Ignore les erreurs en lien avec l'utilisation d'algorithmes dépréciés (exemple : signature de certificat en SHA-1). |
| *httpErreurRenvoiHTTP* Renvoi vers un serveur non sécurisé. | *httpIgnoreRenvoiHTTP* | Le renvoi vers un serveur non sécurisé est autorisé. |
| *httpErreurRenvoiHTTPS* Renvoi vers un serveur sécurisé. | *httpIgnoreRenvoiHTTPS* | Le renvoi vers un serveur sécurisé est autorisé. |
|  | *httpIgnoreRenégociationNonSecurisée* | Ignore l'erreur stipulant que le serveur ne supporte pas la renégociation sécurisée (RFC 5746). |
|  | *httpIgnoreRévocation* | Le contrôle du certificat dans la liste des certificats révoqués n'est pas effectué. |

Remarques :

- Lorsque des requêtes HTTP sont faites dans plusieurs threads, la variable **HTTP.IgnoreErreur** possède une valeur spécifique par thread.

## Voir Aussi

- [HTTPEnvoie (Fonction)](1000021183.md)
- [HTTPEnvoieAsynchrone (Fonction)](1410091208.md)
