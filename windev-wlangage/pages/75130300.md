---
title: "Oui,Non (Langage externe)"
english: "YesNo"
type: "Langage externe"
id: "75130300"
module: "ExternalLanguages"
category: "fonctions_objet"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# Oui,Non (Langage externe)

> **Disponible :** **WINDEV** (Windows)

Affiche une boîte de dialogue avec un sigle, un message et les boutons "oui" et "non". L'utilisateur doit valider un des deux boutons "Oui" ou "Non" pour fermer la fenêtre.
Si la réponse est "Oui", WdChaine contient "O" et WdEntier contient 1.

Si la réponse est "Non", WdChaine contient "N" et WdEntier contient 0.

## Exemple

```wlangage
// En CAPPELWD("OuiNon,Voulez-vous quitter le programme");if WDEntier=1 Quitte_Programme();
```

```wlangage
// En PascalAPPELWD('OuiNon,Voulez-vous quitter le programme');if WdEntier=1 then Quitte_Programme;
```

```wlangage
// En VBcall APPELWD("OuiNon,Voulez-vous quitter le programme")if WdEntier=1 then gosub Quitte_Programme
```

## Syntaxe

```wlangage
Oui,Non(<Message>)
```

`<Message>` *Chaîne de caractères*
> Message à afficher (200 caractères maximum).

## Remarques

Pour afficher le message sur plusieurs lignes, il faut utiliser la syntaxe suivante:
Exemple en C :

```wlangage
APPELWD("OuiNon,  \n ");
```

Exemple en Pascal :

```wlangage
APPELWD('OuiNon, '+Chr(10)+' ');
```

Exemple en VB :

```wlangage
call APPELWD("OuiNon, "+CHR(10)+"")
```

## Voir Aussi

- [Fonctions de gestion des objets (Langage externe)](75130327.md)
