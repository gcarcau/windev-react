---
title: "Confirmer (Langage externe)"
english: "Confirm"
type: "Langage externe"
id: "75130256"
module: "ExternalLanguages"
category: "fonctions_objet"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# Confirmer (Langage externe)

> **Disponible :** **WINDEV** (Windows)

Affiche une boîte de dialogue comportant un sigle, un message, les boutons "Oui", "Non" et "Annuler" et retourne le bouton validé par l'utilisateur.

## Exemple

```wlangage
// En CAPPELWD("Confirmer, Sauvegarde des informations saisies");if WDEntier==1 Sauve();
```

```wlangage
// En PascalAPPELWD('Confirmer, Sauvegarde des informations saisies');if WdEntier=1 THEN Sauve();
```

```wlangage
// En VBcall APPELWD("Confirmer, Sauvegarde des informations saisies")if WdEntier=1 THEN Call SauveEndIf
```

## Syntaxe

```wlangage
Confirmer(<Message>)
```

`<Message>` *Chaîne de caractères*
> Message à afficher (200 caractères maximum).

## Remarques

- WdChaine contient la réponse de l'utilisateur:
  - si la réponse est "Oui", WdChaine contient "O" et WdEntier contient 1.
  - si la réponse est "Non", WdChaine contient "N" et WdEntier contient 0.
  - si la réponse est "Annuler", WdChaine contient "A" et WdEntier contient 2.

- Pour afficher le message sur plusieurs lignes, il faut utiliser la syntaxe suivante :
  - Exemple en C :
    ```wlangage
    APPELWD("Confirmer,  \n ");
    ```
  - Exemple en Pascal :
    ```wlangage
    APPELWD('Confirmer, '+Chr(10)+' ');
    ```
  - Exemple en VB :
    ```wlangage
    call APPELWD("Confirmer, "+CHR(10)+"")
    ```

## Voir Aussi

- [Fonctions de gestion des objets (Langage externe)](75130327.md)
