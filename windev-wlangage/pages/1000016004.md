---
title: "NetMACVersIP (Fonction)"
english: "NetMACToIP"
type: "Fonction"
id: "1000016004"
module: "WDLang3"
category: "fonctions_net"
source_help: ["WINDEV", "WEBDEV", "WINDEV Mobile"]
---

# NetMACVersIP (Fonction)

> **Disponible :** **WINDEV** (Windows, Etats et Requêtes, Code Utilisateur (MCU)) · **WEBDEV** (Windows) · **Autres** (Procédures stockées)

Renvoie l'adresse IP correspondant à une adresse MAC donnée.

## Exemple

```wlangage
// Renvoie l'adresse IP
Info(NetMACVersIP("11:22:33:44:55:66"))
```

## Syntaxe

```wlangage
<Résultat> = NetMACVersIP(<Adresse MAC> [, <Délai>])
```

`<Résultat>` *Chaîne de caractères*
> - Adresse IP obtenue (au format xxx.xxx.xxx.xxx),
> - Chaîne vide ("") en cas de problème.
`<Adresse MAC>` *Chaîne de caractères*
> Adresse MAC au format "aa:bb:cc:dd:ee:ff" ou au format "aa-bb-cc-dd-ee-ff".
`<Délai>` *Entier optionnel*
> Durée maximale (en millisecondes) pendant laquelle la conversion de l'adresse MAC vers IP peut être réalisée. Ce paramètre peut correspondre à :
>
> - un entier correspondant au nombre de millisecondes,
> - une variable de type Durée,
> - l'indication directe de la durée (par exemple 1 s ou 10 ms).
>
> Si ce paramètre n'est pas précisé, cette valeur vaut 10000 (10 secondes).

## Remarques

### Adresse MAC et adresse IP
L'adresse MAC est l'adresse physique spécifiée sur les cartes réseau des postes MAC. L'adresse IP est l'adresse permettant de communiquer entre plusieurs postes via le protocole IP (Internet Protocol).
**Remarque** : Cette fonction ne peut être utilisée que pour des machines reliées en réseau local.

## Liste des exemples associés :

| [image: Les fonctions réseaux (NetXXX)] | Exemples unitaires (WINDEV) : Les fonctions réseaux (NetXXX)     [ + ] Utilisation des fonctions Netxxx sur les adresses réseau. Il est possible de : - récupérer l'adresse IP - récupérer l'adresse MAC - transformer une adresse IP en adresse MAC - transformer une adresse MAC en adresse IP |
| --- | --- |

**Composante : wd300com.dll**

## Voir Aussi

- [NetIPVersMAC (Fonction)](1000016005.md)
- [NetAdresseMAC (Fonction)](1000016006.md)
